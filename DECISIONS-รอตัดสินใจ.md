# สรุปสถานะโครงการ SBPGI — งานที่ยังไม่ได้ทำ และเรื่องที่รอตัดสินใจ

**ปรับปรุง:** 2026-08-24 · **ขอบเขต:** ระบบประกันรายได้ (K2 → SBPGI)
**ที่มา:** SDD GI (สไลด์ 43–66) · ผัง To-Be `120226` · ฐานข้อมูลจริง `sps_store` · living docs (`workflow.md` · `api.md` · `database.md`) · [`TOBE-flow-gap-review-2026-08-19.md`](TOBE-flow-gap-review-2026-08-19.md) · [`SDD-บัญชีประกันรายได้-gap-analysis.md`](SDD-บัญชีประกันรายได้-gap-analysis.md)

> เอกสารนี้มี **2 ส่วน** — ส่วนที่ 1 คือ**งานที่ยังไม่ได้ทำ** (รู้ว่าต้องทำ แต่ยังไม่ได้ลงมือ) · ส่วนที่ 2 คือ**เรื่องที่ยังตัดสินใจไม่ได้** (ลงมือไม่ได้จนกว่าจะมีคำตอบ)
> ทั้งสองส่วนต่างกันคนละเรื่อง — งานในส่วนที่ 1 ส่วนใหญ่**เริ่มได้เลย** ไม่ต้องรอใคร

---

# 📊 สถานะรวม

| ชั้นงาน | สถานะ | หมายเหตุ |
|---|---|---|
| **แบบ (design)** | ✅ **เสร็จ** | living docs 3 ฉบับ + LLDD **40 ฉบับ** + prototype **21 หน้า** + ชุดแผนภาพ flow 4 รูป |
| **ระบบจริง (FE/BE/Job)** | ❌ **ยังไม่เริ่มเขียนสักบรรทัด** | ประเมิน **824 ชม.** (implementation 671 + unit test 153) @ **8 ชม./วัน** ≈ 103 man-day · เพดานทีม 6 คน × 4 สัปดาห์ = 960 ชม. |
| **ย้ายข้อมูล (migration)** | ❌ **ยังไม่ทำ** | ออกแบบเสร็จใน `LLDD-BE-Data-Migration-Cutover` แต่ยังไม่ได้ลงมือ · **F8/F1 บล็อกอยู่** |
| **เรื่องที่รอตัดสินใจ** | 🟠 **15 ข้อ** | ในนั้น **1 ข้อบล็อกการเริ่มเขียน BE** |

---

# ส่วนที่ 1 · งานที่ยังไม่ได้ทำ

## 1.1 🔴 งานพัฒนาระบบจริง — ยังไม่เริ่ม

ทุกอย่างวันนี้เป็น **prototype HTML + เอกสารแบบ** ยังไม่มีโค้ดของระบบจริง

| ปลายทาง | ที่อยู่ | สถานะ |
|---|---|---|
| **FE** | โมดูลใน Next.js portal เดิม (`srm-sps-spsap-web-frontend` · portal `sbpm`) | ยังไม่เริ่ม |
| **BE** | `srm-sps-spsap-store-backend` (NestJS + TypeORM · **มติ DP-10**) | ยังไม่เริ่ม |
| **Job 2–10 + 8b** | batch เดิม → ย้ายมา BE ใหม่ (ตัด Job 1 ImportQSSI แล้ว) | ยังไม่เริ่ม |
| **DDL 19 ตาราง + reuse 1** | `LLDD-Database.md` | เขียนสคริปต์แล้ว · **ยังไม่ deploy** |
| **Unit test** | ratio BE/Job 30% · FE 25% (ยกเว้น 7 เอกสารใน `NO_UNIT_TEST_DOCS`) | ยังไม่เริ่ม |

## 1.2 🟠 ของที่อยู่ในผัง To-Be / SDD แต่ยังไม่มีในแบบของเรา

เรียงจากใหญ่ไปเล็ก — ตัวเลข **B/C** อ้างอิง [`TOBE-flow-gap-review-2026-08-19.md`](TOBE-flow-gap-review-2026-08-19.md)

> ### ✅ มติ 2026-08-24 — ไฟล์กับ IAS/MIS ย้ายจาก SFTP ไป EAI S3
> | เรื่อง | มติ | ผล |
> |---|---|---|
> | ช่องทางแลกไฟล์กับ IAS/MIS | **วางไฟล์บน EAI S3 แทน SFTP ตรง** | Job 4 อัปโหลด `AMS06001O` ขึ้น prefix ขาออก · Job 5 ดึง `AMS06001I` จาก prefix ขาเข้าแล้วย้ายไป prefix backup · **ไม่ต้องมี SFTP credential/known_hosts กับปลายทางนี้อีก** · ALLMAP และ STA **คงกลไกเดิม** |
>
> ### ✅ มติ 2026-08-24 — Job 6 ส่งค่าชดเชยไป STA ด้วย RabbitMQ แทนไฟล์ + SFTP
> | เรื่อง | มติ | ผล |
> |---|---|---|
> | ช่องทางส่งค่าชดเชยไป STA | **publish RabbitMQ message แทนไฟล์ `FRBC0001` + SFTP** | Job 6 เขียน outbox `interface_transactions` (`direction = OUT` · `status = READY`) ใน transaction เดียวกับ 10 mutation → publish exchange `sbpgi.interface` routing `sta.compensation.result` → update `READY → SENT` · เนื้อข้อมูลคงสัญญาเดิม **14 ฟิลด์** แต่เป็น **JSON UTF-8** (ไม่มี windows-874 แล้วเพราะไม่ใช่ไฟล์) · ACK ยังมาทาง `POST /interfaces/sta/ack` และ Job 10 ยังเป็น safety net |
> | ขอบเขต transaction | **แยก commit — เลิกกติกา "SFTP ล้มเหลว = rollback ทั้ง transaction"** | ใช้ transactional outbox แบบเดียวกับ Job 4 · publish ไม่สำเร็จ = แถวค้าง `READY/FAILED_RETRY` ให้ dispatcher ส่งซ้ำ **โดยไม่ rollback การ sync สถานะ** · ส่งซ้ำปลอดภัยเพราะใช้ `message_id = interface_transactions.id` เป็น idempotency key ฝั่ง STA |
>
> ⏳ **ยังต้องยืนยันกับทีม STA/EAI ก่อนลงมือ:** ชื่อ exchange + routing key จริง (ที่ใส่ไว้เป็นค่าเสนอ) · ใครประกาศ exchange/queue และ binding · **ฟิลด์วันที่ 3/5/6 ยังต้องเป็น พ.ศ. หรือเปลี่ยนเป็น ISO ค.ศ. ได้** (ข้อจำกัด windows-874 หายไปแล้ว แต่รูปแบบวันที่เป็นสัญญาข้อมูล ไม่ใช่ข้อจำกัดของไฟล์) · STA กันรับซ้ำด้วย `message_id` ได้จริงหรือไม่ · นโยบาย DLQ/retry ของ broker · ยังต้องส่งไฟล์คู่ขนานช่วง cutover หรือไม่

> ⏳ **ยังต้องถามฝั่ง EAI ก่อนลงมือ:** ชื่อ bucket + โครง prefix (ขาออก/ขาเข้า/backup) · วิธียืนยันสิทธิ์ (IAM role ของ pod หรือ access key ใน secret) · **ใครลบอ็อบเจกต์เก่า** (lifecycle policy ของ EAI หรือ SBPGI ทำเอง) · IAS/MIS รู้ได้อย่างไรว่ามีไฟล์ใหม่ (poll เอง หรือมี event/notification)

> ### ✅ ปิดเพิ่ม 2026-08-24 — ตัด Job 1 ImportQSSI ออกจากขอบเขต
> | ข้อ | มติ | ผล |
> |---|---|---|
> | **DP-4** · `fcs_qssi_score` reuse ตารางเดิม 23.9M แถว หรือสร้างใหม่ | **reuse ตารางเดิมแบบอ่านอย่างเดียว** — ระบบ SBP เดิมนำเข้าให้แล้วผ่าน `POST /performance/import-qssi` | **ตัด Job 1 (ImportQSSI) ทั้ง job** · ไม่ต้อง backfill · ไม่ต้อง `SET NOT NULL` · **ไม่ต้องขอ sign-off เจ้าของ `performance.service.ts`** เพราะ SBPGI ไม่เขียนตารางนี้เลย |
> 
> **ผลต่อชุดส่งมอบ:** LLDD **41 → 40 ฉบับ** · Batch Job **11 → 10 entry point** · ชั่วโมง **845 → 824** (implementation 671 + unit test 153)

> ✅ **ทำไปแล้ว 2026-08-24 (7 ข้อ)** — **B5** ร้านกระทบเพิ่ม 2 ทาง (`source_system` = `ALLMAP` auto / `USER` คีย์เอง) + ปุ่ม **เพิ่มร้านกระทบเพิ่ม** ใน `k2-document.html` + กติกาเกลี่ย %ชดเชยใหม่ทั้งชุด · B4 เกณฑ์เลือกปุ่มปฏิเสธ + เหตุผล "Location ที่ไม่เหมาะสม" (`workflow.md` · `k2-document.html`) · B6 ตารางเทียบชื่อ lane ผัง ↔ section (`workflow.md`) · B7 ปลายทางบัญชี → SAP อยู่นอก workflow (`workflow.md`) · B9 `STA-SS` = `STA` ระบบเดียวกัน (`workflow.md` · `k2-flow.html`) · C4 checklist บัญชี 5 ข้อ + สาเหตุยอดไม่ตรง 2 ข้อ (`api.md`) · C5 ตัวเลข sizing/NFR 150–170 ฉบับ/เดือน (`api.md`)

| # | เรื่อง | ขนาด | ทำไมยังไม่ทำ | เริ่มได้เลยไหม |
|---|---|---|---|---|
| **B3** | **หน้างานค้างมี 2 สถานะ*ก่อน*เข้า workflow** — `รอมอบหมายงาน` (ระบบดึงครั้งแรก) · `คงค้าง` (เห็นควรไม่ชดเชย) | 🔴 **ใหญ่ · เชิงสถาปัตยกรรม** | `GET /tasks` อ่านจาก `getPendingFlowByUser()` ของ engine ซึ่งคืนเฉพาะงานที่**มี workflow instance แล้ว** — แต่ผังต้องการให้แสดงร้านที่ยัง**ไม่มีเอกสาร**ด้วย → ต้องมีแหล่งข้อมูลที่ 2 แล้ว merge | ⚠️ ต้องเคาะขอบเขตก่อน (กระทบทั้ง schema + API) |
| **C3** | **คำนวณชดเชยแยกตามประเภทร้าน** (`Type A,C` / `Type B,E,V(B)`) + Performance Index · Incentive Guarantee · GP ก่อน Split · ส่วนแบ่งรายได้ตามเป้า (สไลด์ 33 · 36) | 🔴 **ใหญ่** | ยังไม่เคยแตะเลย · เป็นสูตรคำนวณเงิน | ⚠️ ต้องเคาะขอบเขต — เสนอเป็นเฟส 2 |

## 1.3 🧹 หนี้ทางเทคนิคใน prototype

| เรื่อง | รายละเอียด | ผลถ้าไม่ทำ |
|---|---|---|
| **dead code ใน `assets/sbp.js`** | โหมด `data-nav="application"` (บรรทัด ~160–170) + `switchStep()` (~386) ยังลิงก์ไป `recruitment.html` · `application.html` · `application-step1/3.html` ซึ่ง**ถูกลบไปหมดแล้ว** | ไม่พังตอนนี้ แต่คนอ่านโค้ดจะเข้าใจผิดว่ามีหน้าเหล่านั้น · CLAUDE.md เขียนกำกับไว้ว่า *"Do not use or copy that mode"* |
| **หน้า reference ที่ถอดจาก sidebar** | `k2-permissions.html` · `k2-operators.html` เก็บไว้อ้างอิงแต่ไม่มีทางเข้า | ตั้งใจ — RBAC ใช้ของระบบ SBP เดิม |

---

# ส่วนที่ 2 · เรื่องที่ยังต้องตัดสินใจ (15 ข้อ)

**แบ่ง 4 กลุ่มตามความเร่งด่วน** — ต้องตัดสินก่อนเขียนโค้ด **1** · กระทบ schema/สัญญา **6** · ตัดสินทีหลังได้ **3** · ต้องไปถามทีมอื่น **5**

## 🔴 กลุ่ม 1 — ต้องตัดสินก่อนเริ่มเขียนโค้ด (1 ข้อ)

### 1.1 · DP-11 — ตัวเลข "เงินประกันรายได้" SBPGI เป็นต้นทาง หรือ `fr_store_insure` ยังคีย์มือ ?
| | |
|---|---|
| **ทางเลือก** | (ก) SBPGI เขียนตัวเลขลง `fr_store_insure` เอง · (ข) ทีมงานยังคีย์มือเหมือนเดิม |
| **บล็อกอะไร** | ตาราง `compensation_histories` **สร้างไม่ได้** จนกว่าจะเคาะ · กระทบ Job 6 (message ที่ส่งไป STA) |
| **ประเภท** | **ธุรกิจล้วน** ไม่ใช่เทคนิค |
| **⚠️ ไม่ใช่เรื่องเดียวกับเกณฑ์ 100,000** | `100,000` = **วงเงินอนุมัติ** (ใครเซ็น) ปิดไปแล้ว 2026-08-18 · **DP-11 = ตัวเลขเงินที่อนุมัติแล้วไปเก็บที่ไหน** · ระบบเดิมมี `sps_store.fr_store_insure` (708 แถว · `store_id`·`year`·`month`·`money_support`·`split` ต่อ `order_id`) ที่โมดูล inquiry เขียนอยู่แล้ว และ**มิติซ้ำ**กับ `compensation_histories` ที่เราจะสร้าง |
| **ต้องถามฝั่งบัญชี/SBP ก่อนตอบ** | `fr_store_insure` วันนี้ **ใครกรอก · กรอกจากอะไร · เป็นตัวเลขเดียวกับเงินชดเชยที่ผ่าน workflow เราไหม** |
| **ใครตอบ** | เจ้าของกระบวนการฝั่งบัญชี/SBP |

---

## 🟠 กลุ่ม 2 — ตัดสินได้ระหว่างทาง แต่กระทบ schema / สัญญา API (6 ข้อ)

| # | เรื่อง | ทางเลือก | กระทบ |
|---|---|---|---|
| **2.1** | **DP-13** — `BranchTypeFGIName` ของ `Type V(C)` และ `Type V(B)` <br><small>(นิยามอยู่ใน `database.md` · ไม่ใช่ทะเบียน DP-1…DP-12 ของ `SBP/`)</small> | คงเป็น `C` ทั้งคู่ · หรือได้รหัสใหม่คนละตัว | ตัวกรองประเภทร้านในหน้ารายงาน (ตอนนี้ 7 ค่า) · master mapping · **SDD สไลด์ 36 จัดกลุ่มสูตรคำนวณเป็น `Type B, E, V(B)` ซึ่งบ่งว่า V(B) ไม่ใช่ C** |
| **2.2** | **DP-13b** — ชื่อแสดงผลของ `E` (= B(2)) <br><small>(พบ 2026-08-24 ตอนเติม mock data ประเภทร้าน)</small> | รายการชื่อแสดงผลเป็น **10 ชนิด** (เพิ่ม `SBP Type E`) · หรือ `E` ใช้ชื่อแสดงผลอื่นที่มีอยู่แล้ว | `E` มีจริงใน master `BranchTypeFGIName` (7 ค่า `A B C D E PTT บริษัท`) และมีในตัวกรองหน้ารายการ/รายงาน แต่ **ไม่อยู่ในรายการชื่อแสดงผล 9 ชนิด** ของ `database.md` · prototype ใช้ `SBP Type E` ไปพลางก่อน |
| **2.3** | **คอลัมน์ที่วิเคราะห์แล้วว่าต้องมี แต่ยังไม่ได้เติมลง DDL** <br><small>(พบ 2026-08-24 ตอนไล่ `table.column` ทุกไฟล์)</small> | เติมลง DDL · หรือยืนยันว่าไม่ต้องมี | **`compensation_documents.approver_snapshot`** (JSONB — ชื่อ+อีเมล DV/GM/AVP ณ เวลาเปิดเอกสาร · `database.md` สรุปจากไฟล์ `BPM06001O_` ที่มี 24 ฟิลด์ผู้อนุมัติว่า *"ต้องมี"*) · **`impacted_stores.transfer_sbp_date`** (เงื่อนไขร้านก่อน/หลัง 1/10/2014) · ทั้งคู่ถูกอ้างในตาราง PDPA และ mapping แต่ **ไม่มีใน `CREATE TABLE`** |
| **2.4** | **DP-6** — `interface_transactions` ออกแบบใหม่ หรือลอกแพตเทิร์น `statement_summary` | — | DDL + การ track ACK ของ interface |
| **2.5** | **N1** — "หลังพัฒนา **ไม่มีการเปิด SR เพื่อลบข้อมูล**" (SDD สไลด์ 46 · Note) | **ยังไม่ระบุขอบเขตเลย** — ลบอะไรได้บ้าง (เอกสาร? แถวร้าน?) · ใครลบได้ · ต้องมี audit ไหม | หน้าจอ + endpoint ใหม่ที่ยังไม่มีในแผน · ระบบเดิมมี log `TransectionDeleteStore` + `SRNumber` เป็นหลักฐานว่าเคยต้องเปิด SR |

| **2.6** | **PDPA Requirement (SDD สไลด์ 63)** — SDD ระบุว่างานนี้มี Normal Personal Data (ชื่อ-นามสกุล · ที่อยู่/อีเมล/โทรศัพท์) และสั่งให้ *"เพิ่มเติมรายละเอียดตาม Template ของ PDPA Requirement ตามเอกสารแนบ"* | ยังไม่มีเอกสาร PDPA Requirement ในชุดส่งมอบ · ต้องเคาะ **retention ของข้อมูลบุคคล · การ mask ใน log · สิทธิ์เข้าถึง `approver_snapshot` (ชื่อ+อีเมลผู้อนุมัติ)** | ข้อมูลบุคคลในตารางเรา: `consider_by` · `created_by`/`updated_by` · `approver_snapshot` · `file_name` (รายละเอียดใน `database.md` §PDPA) — ที่เหลืออยู่ในระบบ SBP เดิม |

---

## 🟡 กลุ่ม 3 — ตัดสินทีหลังได้ (3 ข้อ)

| # | เรื่อง | หมายเหตุ |
|---|---|---|
| **3.1** | **DP-12** — audit ของ master data: เอากลับมาโดยใช้กลไกของระบบเดิม หรือไม่มีเลยตามมติ 2026-08-07 | `audit_logs` ถูกตัดไปแล้ว |
| **3.2** | **C3 (P2)** — คำนวณชดเชย**แยกตามประเภทร้าน** (`Type A,C` / `Type B,E,V(B)`) + Performance Index · Incentive Guarantee · GP ก่อน Split · ส่วนแบ่งรายได้ตามเป้า (SDD สไลด์ 33 · 36) | **งานใหญ่** ยังไม่ได้ทำเลย · ต้องเคาะขอบเขตก่อน |
| **3.3** | **ขอบเขตหน้างานค้าง (B3)** — จะทำ 2 สถานะก่อนเข้า workflow (`รอมอบหมายงาน` / `คงค้าง`) ในเฟสนี้ หรือเลื่อนไปเฟส 2 | ต้องเพิ่มแหล่งข้อมูลที่ 2 (ร้านที่เข้าเงื่อนไขแต่ยังไม่มีเอกสาร) แล้ว merge เข้ากับ pending list ของ engine · **กระทบทั้ง schema และ `GET /tasks`** · รายละเอียดงานดู [ส่วนที่ 1 ข้อ 1.2](#12--ของที่อยู่ในผัง-to-be--sdd-แต่ยังไม่มีในแบบของเรา) · ส่วน B4/B5/B7/B9 เป็นงานที่ทำได้เลย ไม่ต้องตัดสินใจ |

---

## 📞 กลุ่ม 4 — ไม่ใช่การตัดสินของคุณ แต่ต้องไปถาม/ตาม (5 ข้อ)

| # | เรื่อง | ถามใคร | ความเสี่ยงถ้าไม่ถาม |
|---|---|---|---|
| **4.1** | **DP-2** — `sps_store.workflow_transaction` **ไม่มี PK และไม่มี index เลย** ทั้งที่มี 19,283 แถว (ตารางชื่อเดียวกันใน `sps_auth` มี PK ปกติ) | ทีมเจ้าของ `@srm/glb-workflow` | `GET /tasks` และ `POST /documents/{docNo}/actions` query ตารางนี้**ทุกครั้ง** → seq-scan ทุกคำขอ |
| **4.2** | **engine ส่งอีเมลเองด้วยหรือไม่** บน route ที่มี `email_id` | ทีมเจ้าของ `@srm/glb-workflow` | ถ้าส่งเอง ผู้อนุมัติจะได้ **เมลซ้ำ 2 ฉบับ** (เพราะ SBPGI เรียก `sendEmail()` เองตามมติ DP-5) |
| **4.3** | **ชื่อ package + transport ของ email-lib** <br><small>(อ่านสเปกจริง `SBP/TSM-SRM-LLDD SBP EMAIL1.0.xlsx` แล้ว 2026-08-24 — **สัญญาเรียกใช้ยืนยันครบ** เหลือ 2 เรื่อง)</small> **(1)** เอกสาร lib **ไม่ได้ระบุชื่อ package** เลย (โครงไฟล์เขียนแค่ `email/`) — `@gosoft-sbp/email-lib` ยังเป็นชื่อที่เดาจากเอกสารอื่น · **(2)** เอกสาร lib **ขัดกันเองในไฟล์เดียว** — ผังไฟล์เขียน *“ส่งเมล์ผ่าน AWS SES”* แต่ลำดับขั้นตอนเขียน *“ส่งเมล์ไปที่ server mail SMTP”*| ทีมเจ้าของ email-lib | ติดตั้ง/เรียกใช้ผิด · ถ้าเป็น SES ต้องมี IAM role + verified identity ซึ่งเป็นงาน infra คนละชุดกับ SMTP |
| **4.4** | **ALLMAP ใช้ปี พ.ศ. หรือ ค.ศ.** — argument ของ Job 2/3 เป็น `2569\|06` (พ.ศ.) แต่ระบบตกลงใช้ ค.ศ. ทั้งระบบ | เจ้าของ ALLMAP | Job 2/3 ดึงข้อมูลผิดงวด |
| **4.5** | **`workflow_part` / `workflow_part_display`** ของ engine คุมสิทธิ์แสดงผลรายส่วนหน้าจอ ซึ่ง**ทับซ้อน**กับกลไก `visibleSections`/`editableSections` ที่ SBPGI ทำเอง | ทีมเจ้าของ `@srm/glb-workflow` | ถ้าใช้ของ engine จะกระทบ `LLDD-FE-Document-Detail` + role pack 5 ฉบับ |

---

# 🗺️ ลำดับที่แนะนำ

| ลำดับ | ทำอะไร | ข้อ |
|---|---|---|
| **1** | **ตอบก่อนเริ่ม BE** — กระทบ DDL โดยตรง | `1.1` DP-11 (ข้อเดียวที่บล็อกการเริ่ม BE) |
| **2** | **ยิงคำถามคู่ขนานได้เลย** ไม่ต้องรอข้ออื่น | กลุ่ม 4 ทั้ง 5 ข้อ |
| **3** | **ก่อนเริ่ม FE** | `2.1` DP-13 · `2.2` DP-13b |
| **4** | ~~ลุยงานที่ไม่ต้องรอใคร~~ **ทำแล้ว 2026-08-24** | ~~B4 · B6 · B7 · B9 · C4 · C5~~ |
| **5** | **เคาะขอบเขตเฟส 2** | `3.2` C3 คำนวณแยกประเภทร้าน · `2.5` N1 การลบข้อมูล · `3.3` B3 หน้างานค้าง |

---

# ✅ ประวัติการปิดข้อค้าง

> ### ✅ ปิดเพิ่ม 5 ข้อ (2026-08-24) — ตัดสินได้จาก SDD เอง หรือจากมติที่ให้ไว้แล้ว ไม่ต้องถามใหม่
> | ข้อ | มติ | หลักฐาน |
> |---|---|---|
> | **B8** · To-Be ยังมีขั้น AVP (03) ไหม | **มี — คงขั้น 03 ไว้ ไม่ต้องแก้อะไร** | มติ 2026-08-18 (เกณฑ์เดียว 100,000 · ≥ 100,000 → AVP) เป็นมติที่ให้ไว้แล้ว + SDD สไลด์ 54 · 55 · 62 มี AVP · ผัง To-Be `120226` ตกหล่นข้างเดียว |
> | **D6** · ชื่อสถานะขั้น 03 | **ใช้ของเดิม `รอผู้บริหารสำนักบริหาร SBP ดำเนินการ`** ไม่ rename | SDD เรียกหน่วยงานนี้ว่า **"สำนักบริหาร Store Business Partner" 4 ครั้ง** ในตารางผู้เกี่ยวข้อง (Project Sponsor/Owner/BPM/Business User) ส่วน "สำนักงานบริการ" โผล่**ครั้งเดียว**ในลิสต์สถานะสไลด์ 62 → เป็น typo ของ SDD ไม่ใช่มติเปลี่ยนชื่อ |
> | **DP-7** · `consideration_logs` ตารางเราเอง หรือต่อยอด `workflow_history` | **ตารางของเราเอง** (ผูก `transaction_id` ของ engine) | ตัดสินไปแล้วตั้งแต่รอบ reuse 2026-08-06 · บันทึกอยู่ใน `database.md` — engine ไม่มี `decision_code` / `result_category` / ไฟล์แนบต่อการพิจารณา · เป็นรายการที่ตกค้างในทะเบียนทั้งที่มีมติแล้ว |
> | **DP-8** · `document_attachments` ตารางเราเอง หรือต่อยอด `upload_general` | **ตารางของเราเก็บ metadata · ใช้ service S3 ของระบบเดิม ไม่เขียน storage layer เอง** | ตัดสินไปแล้ว 2026-08-06 · บันทึกอยู่ใน `database.md` (`upload_general` ขาดฟิลด์ที่ต้องใช้) |
> | **B2** · 3 ช่องทางต้นทาง (เชิงรุก/เชิงรับ/ระบบ) | **ตั้งรหัสใหม่ `PRO` = เชิงรุก · `REA` = เชิงรับ** เพิ่มเข้า `fgi_impact_processes.datasource` (เดิมมี `ALM`/`STA`) · seed ที่ `common_code` code_type `SBPGI_DATASOURCE` | ตรวจแล้ว**ไม่มีที่ไหนกำหนดรหัสไว้** — SDD 0 hit · Java เดิมมีแค่ `ALM`/`STA`/`HRS` · ไม่มีข้อมูลฝั่งระบบเดิม → ตั้งตามแพตเทิร์น 3 ตัวอักษรของเดิม (อนุมัติ 2026-08-24) |
> | **C1 / C2** · checkbox เลือกหลายเอกสาร + ปุ่ม Preview Report / Export CSV to Batch | **ทำแล้ว 2026-08-24** | SDD ระบุตรง ๆ ไม่ต้องตีความ — สไลด์ 48 *"แสดง Chk list สำหรับเลือกดำเนินการหลายเอกสาร"* + *"หน้าจอ popup เมื่อยืนยัน"* · สไลด์ 62 ปุ่ม **Preview Report** และ **Export CSV to Batch** |


> ### ✅ ปิดไปแล้ว 3 ข้อ (2026-08-21)
> | ข้อ | มติ |
> |---|---|
> | **DP-10** · SBPGI อยู่ที่ไหน | **ใส่ใน `srm-sps-spsap-store-backend` เดิม** — ไม่แยก backend ใหม่ |
> | **F8 + F1** · ขนคอลัมน์/ตารางจากระบบเดิม | **ทำ** — เพิ่มตาราง `fgi_impact_compensations` + 5 กลุ่มคอลัมน์ใน `fgi_impact_processes` · โครงเป็น **20 ตาราง** |
> | **ชั่วโมงงาน** | **ใช้ชุด LLDD `845 ชม.` @ 8 ชม./วัน (กรอบใหม่ 2026-08-25)** (→ **824 ชม.** หลังตัด Job 1 เมื่อ 2026-08-24) (823 + 22 หลังรับ F8+F1 และงานจุดเข้า flow) · ตรวจแล้วสองชุดขอบเขตเดียวกัน ต่างกันแค่หน่วย (142.0 vs 140.8 man-day = 0.8%) · ไฟล์ estimate เดิมเป็น baseline superseded |

> เอกสารนี้รวม**เฉพาะข้อที่ยังไม่ตัดสิน** · ข้อที่ตัดสินไปแล้ว (DP-1 · DP-3 · DP-5 · DP-9 · วงเงิน 100,000 · ลำดับ 06→08→01 · คำเรียกประเภทร้าน) บันทึกอยู่ใน living docs ตามเดิม
