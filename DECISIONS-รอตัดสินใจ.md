# สรุปสถานะโครงการ SGI — งานที่ยังไม่ได้ทำ และเรื่องที่รอตัดสินใจ

**ปรับปรุง:** 2026-09-17 · **ขอบเขต:** ระบบประกันรายได้ (K2 → SGI)
**ที่มา:** SDD GI (สไลด์ 43–66) · ผัง To-Be `120226` · ฐานข้อมูลจริง `sps_store` · living docs (`workflow.md` · `api.md` · `database.md`) · [`TOBE-flow-gap-review-2026-08-19.md`](TOBE-flow-gap-review-2026-08-19.md) · [`SDD-บัญชีประกันรายได้-gap-analysis.md`](SDD-บัญชีประกันรายได้-gap-analysis.md)

> เอกสารนี้มี **2 ส่วน** — ส่วนที่ 1 คือ**งานที่ยังไม่ได้ทำ** (รู้ว่าต้องทำ แต่ยังไม่ได้ลงมือ) · ส่วนที่ 2 คือ**เรื่องที่ยังตัดสินใจไม่ได้** (ลงมือไม่ได้จนกว่าจะมีคำตอบ)
> ทั้งสองส่วนต่างกันคนละเรื่อง — งานในส่วนที่ 1 ส่วนใหญ่**เริ่มได้เลย** ไม่ต้องรอใคร

---

# 📊 สถานะรวม

| ชั้นงาน | สถานะ | หมายเหตุ |
|---|---|---|
| **แบบ (design)** | ✅ **เสร็จ** | living docs 3 ฉบับ + LLDD **43 ฉบับ** (42 ฉบับมี .md = 39 หัวข้อ + 3 อ้างอิง · + พจนานุกรมข้อมูลที่เป็น PDF อย่างเดียว) + prototype **21 หน้า** + ชุดแผนภาพ flow 4 รูป |
| **ระบบจริง (FE/BE/Job)** | ❌ **ยังไม่เริ่มเขียนสักบรรทัด** | ประเมิน **829 ชม.** (implementation 675 + unit test 154) @ **8.5 ชม./วัน** ≈ 97 man-day · เพดานทีม 6 คน × 4 สัปดาห์ × 42.5 ชม. = **1,020 ชม.** |
| **ย้ายข้อมูล (migration)** | ❌ **ยังไม่ทำ** | ออกแบบเสร็จใน `LLDD-BE-Data-Migration-Cutover` · สคริปต์ติดตั้งพร้อมแล้วที่ `output/sql/sgi_schema.sql` + `sgi_seed_data.sql` (2026-09-09) แต่ยังไม่ได้ลงมือ · **✅ F8/F1 ไม่บล็อกแล้ว — ปิดตั้งแต่ 2026-08-21 และรับเข้าโครง 20 ตารางเรียบร้อย** · สิ่งที่ยังค้างคือ **DP-11 (ข้อ 1.1)** — ลง schema ได้ครบทุกตาราง แต่ยังตัดสินไม่ได้ว่าใครเขียนข้อมูลลง `sgi_compensation_histories` และยอดที่ Job 6 ส่งไป STA มาจากไหน |
| **เรื่องที่รอตัดสินใจ** | 🟠 **39 ข้อ** | ในนั้น **1 ข้อบล็อกการเริ่มเขียน BE** |

---

# ส่วนที่ 1 · งานที่ยังไม่ได้ทำ

## 1.1 🔴 งานพัฒนาระบบจริง — ยังไม่เริ่ม

ทุกอย่างวันนี้เป็น **prototype HTML + เอกสารแบบ** ยังไม่มีโค้ดของระบบจริง

| ปลายทาง | ที่อยู่ | สถานะ |
|---|---|---|
| **FE** | โมดูลใน Next.js portal เดิม (`srm-sps-spsap-web-frontend` · portal `sbpm`) | ยังไม่เริ่ม |
| **BE** | `srm-sps-spsap-store-backend` (NestJS + TypeORM · **มติ DP-10**) | ยังไม่เริ่ม |
| **Job 2–12 + 8b (12 ตัว)** | batch เดิม 10 ตัว (ตัด Job 1 ImportQSSI แล้ว) + Job 11/12 ที่เพิ่ม 2026-09-02 → ย้ายไป repo `srm-sps-spsap-sop-sgi-batch` | ยังไม่เริ่ม |
| **DDL 19 ตาราง + reuse 1** | `LLDD-Database.md` | เขียนสคริปต์แล้ว · **ยังไม่ deploy** |
| **Unit test** | ratio BE/Job 30% · FE 25% (ยกเว้น 7 เอกสารใน `NO_UNIT_TEST_DOCS`) | ยังไม่เริ่ม |

## 1.0 🔴 แผน 4 สัปดาห์ยังไม่ลงตัว — งานของ Bank เกินเพดาน 41 ชม.

**มติที่รับมา (2026-08-25):** จบใน **4 สัปดาห์ · 5 วัน/สัปดาห์ · 8.5 ชม./วัน = 170 ชม./คน** · Bank ทำ **migration DB + batch job ทั้งหมด + สร้าง workflow (นิยาม)** · คนอื่นแบ่งงานที่เหลือ · BE บางคนรับ `initializeWorkflow` และ trigger event

> **มติ 2026-09-02 (รอบ 4) — Bank ถือ batch job ทั้งหมด · งานที่ไม่ใช่ job ย้ายออก**
> เพิ่มเอกสาร 2 ฉบับปิดช่องว่าง 2.8 / 2.9 → **LLDD 40 → 42 ฉบับ · batch job 10 → 12 · ยอดรวม 800 → 829 ชม.**
> Bank ถือ **เฉพาะ job ทั้ง 12 ฉบับ = 211 ชม.** · ย้าย **Database Structure (31) + Workflow Engine Definition (24) → Pete** และ **Data Migration/Cutover (43) → Vava** ออกจาก Bank
>
> | คน | ชม. | เพดาน 170 |
> |---|---|---|
> | **Bank** (job ทั้งหมด 12 ฉบับ) | **211** | ❌ **เกิน 41 ชม. = 4.96 สัปดาห์** |
> | Vava | 145 | ✅ |
> | Pete | 139 | ✅ |
> | lin | 123 | ✅ · But 108 ✅ · New 103 ✅ |
>
> **⚠️ \"job ทั้งหมดให้ Bank + จบใน 4 สัปดาห์\" ยังขัดกันทางเลขอยู่ 41 ชม.** — ย้ายงานที่ไม่ใช่ job ออกหมดแล้ว (98 ชม.) ก็ยังไม่พอ เพราะลำพัง job ก็ 211 ชม. · ต้องเลือกทางใดทางหนึ่ง:
>
> | ทาง | ทำอะไร | ผล |
> |---|---|---|
> | **A** | คิดตัวคูณ senior+AI **เฉพาะ unit test ×0.6** (ตามมติเดิม · ไม่แตะ impl เพื่อไม่นับส่วนลด reuse ซ้ำ) | **190 ชม. = 4.47 สัปดาห์** — ยังเกิน 20 ชม. |
> | **B** | Bank ทำ **10.6 ชม./วัน** × 20 วัน | ไม่สมจริง |
> | **C** | ยอมให้สาย job จบ **สัปดาห์ที่ 5** (ส่วนอื่นจบใน 4 สัปดาห์ตามเดิม) | ✅ ทำได้จริงโดยไม่ต้องแตะขอบเขต |
> | **D** | ย้าย job ออก ≥ 41 ชม. ให้คนอื่น (เช่น Job 10 + 3 + 7 + 9 = 45) | ขัดกับ \"job ทั้งหมดให้ Bank\" |
> | **E** | ตัด/เลื่อน job ≥ 41 ชม. ไปเฟสถัดไป | ต้องเลือกว่า job ไหนไม่ต้องมีตอน go-live |
>
> **ปรับชั่วโมงตามของที่ reuse ได้ (2026-09-02)** — หลังมติย้าย batch มาทำบน `SBP/srm-sps-spsap-sop-sgi-batch` (มี dispatcher · `integration_log` · `publishMessage` · `S3Service` · `decodeThaiFileContent` · email-lib พร้อมแล้ว) หักลบกับงานใหม่ที่เพิ่งรู้ว่าต้องทำ (join ข้าม schema · INPUT DTO + validation · outbox บน publisher ที่ไม่มี confirm) → **ยอดรวมชุดส่งมอบ 824 → 800 ชม.** · **สาย batch job 217 → 193** · **Bank 296 → 280** · ที่มาของ delta รายฉบับอยู่ในคอมเมนต์ของ `JOB_ESTIMATES` ใน `tools/build_lldd_documents.py`

<details>
<summary>📦 <b>ตารางรุ่นก่อนมติ 2026-09-02 (รอบ 4) — เก็บไว้ดูที่มาเท่านั้น ห้ามใช้ตัดสินใจ</b></summary>

⚠️ **ตัวเลขชุดนี้ตกยุคแล้ว** — เป็นยอดสมัยที่ Bank ยังถือ migration DB + workflow definition และยอดรวมยังเป็น 800 ชม.
**ตัวเลขที่ใช้จริงอยู่ในกล่อง "มติ 2026-09-02 (รอบ 4)" ด้านบน** (Bank 211 · Vava 145 · Pete 139 · lin 123 · But 108 · New 103 · รวม 829)

| คน | ชม. *(ตกยุค)* | = วัน (8.5 ชม.) | สถานะ *(ตกยุค)* |
|---|---|---|---|
| **Bank** | **280** *(ตกยุค)* <small>(เดิม 296)</small> | **32.9** | ❌ **ยังเกินเพดาน 110 ชม. (~6.6 สัปดาห์)** |
| lin | 123 | 14.5 | ✅ |
| But | 108 | 12.7 | ✅ |
| New | 103 | 12.1 | ✅ |
| Vava | 102 *(ตกยุค)* | 12.0 | ✅ |
| Pete | 84 *(ตกยุค)* <small>(เดิม 92)</small> | 9.9 | ✅ |

ที่มาของ 280 ชม. *(ตกยุค)* = migration DB 74 (Database Structure 31 + Data Migration/Cutover 43) + **batch job ทั้งหมด 182** (Jobs 2–12 + 8b รวม 12 ตัว · เดิม 198) + สร้าง workflow 24

**ต้นเหตุยังเป็น batch job 182 ชม.** ไม่ใช่ migration หรือ workflow — สองอย่างหลังรวมกันแค่ 98 ชม. ซึ่งพอดีกับ 4 สัปดาห์อยู่แล้ว · **การ reuse ช่วยได้ 16 ชม. แต่ไม่ได้แก้ปัญหาโครงสร้าง** เพราะงานส่วนใหญ่ของ job คือ business logic ไม่ใช่ infra

**ของจริงหลังมติ 2026-09-02 (รอบ 4):** Bank ไม่ได้ถือ migration/workflow แล้ว — `Database Structure (31)` และ `Workflow Engine Definition (24)` เป็นของ **Pete** · `Data Migration/Cutover (43)` เป็นของ **Vava** · Bank ถือเฉพาะ **เอกสาร job 12 ฉบับ = 211 ชม.** (รวม unit test) จึงเกินเพดาน 170 อยู่ **41 ชม.** ไม่ใช่ 110

</details>

| ทางเลือก | ทำอะไร | ผล |
|---|---|---|
| **A — แบ่ง job ออก ~110 ชม.** | Bank เก็บ job ที่ผูกกับ workflow/ข้อมูลไว้ (8b 28 + Job 2 20 + Job 3 12 = 60) · ที่เหลือ 122 ชม. กระจายให้ Vava/But/Pete ซึ่งว่างรวมกัน ~200 ชม. | Bank เหลือ 158 ชม. ✅ ทุกคนอยู่ใน 4 สัปดาห์ |
| **B — ขยายเวลาเฉพาะสาย job** | คงตัวคนตามมติ แต่ยอมให้สาย job จบสัปดาห์ที่ 7 | ส่วนอื่นเสร็จใน 4 สัปดาห์ · ระบบใช้งานจริงได้เมื่อ job ครบ |
| **C — เพิ่มชั่วโมง/วันของ Bank** | 280 ชม. ÷ 20 วัน = **14.0 ชม./วัน** | ไม่สมจริง |
| **D — ตัดขอบเขต job** | ตัด/เลื่อน job ~110 ชม. ไปเฟสถัดไป | ต้องเลือกว่า job ไหนไม่ต้องมีตอน go-live |
| **E — 4.5 สัปดาห์ (productivity + แบ่ง job)** | คิดตัวคูณ senior+AI **แล้วย้าย 5 job ออก** | Bank เหลือ **185 ชม.จริง = 4.35 สัปดาห์** ✅ |

### รายละเอียดทางเลือก E — ทำไมต้องทำสองอย่างพร้อมกัน

Bank เป็น senior และใช้ AI ได้ดี จึงคิดตัวคูณตามลักษณะงานจริง (ไม่ใช่คูณลอย ๆ):

| ส่วนงาน | ตัวคูณ | เหตุผล |
|---|---|---|
| Unit test (45 ชม. ในสาย job · เดิม 49) | ×0.6 | LLDD ระบุ "ขอบเขต unit test" ต่อ job ไว้ครบแล้ว AI generate จากสเปกได้ตรง |
| Job ที่ 4–10 | ×0.75 | 10 job ใช้ dispatcher เดียวกันใน `src/main.ts` ของ sop-sgi-batch · โครง config→flow→SQL→test ซ้ำกัน · Job 2/3 และ 7/9 เป็นฝาแฝด <br><small>⚠️ ตัวคูณนี้ทับซ้อนกับส่วนลด reuse ที่หักไปแล้วใน `JOB_ESTIMATES` — อย่านับซ้ำ</small> |
| Job 3 ตัวแรก | ×1.0 | ยังต้องวางแพตเทิร์นร่วม (`src/modules/sgi/` · DTO · sgi-job.types) ให้คนอื่นใช้ต่อ |
| Migration (74 ชม.) | ×0.85 | source-to-target mapping เขียนครบใน LLDD แล้ว เหลือ transform + reconcile |
| สร้าง workflow (24 ชม.) | ×0.85 | seed data ชุดเล็ก |

| ทำอะไร | Bank เหลือ | = สัปดาห์ |
|---|---|---|
| ย้าย job อย่างเดียว | **213 ชม.** <small>(เดิม 217)</small> | 5.0 ❌ |
| productivity อย่างเดียว | ~230 ชม. <small>(ต้องคำนวณใหม่ — ดูคำเตือนด้านล่าง)</small> | ~5.4 ❌ |
| **ทั้งสองอย่าง** | ~**175 ชม.** <small>(ต้องคำนวณใหม่)</small> | ~4.1 🟡 |

> ⚠️ **ตัวเลขแถว productivity ต้องคำนวณใหม่** — เดิมคิดบนฐาน 296 ชม. ที่ยังรวมงานสร้าง runner/scheduler/notifier เอง · ตอนนี้ส่วนนั้นถูกหักออกจาก `JOB_ESTIMATES` ไปแล้ว การคูณ ×0.75 ทับอีกรอบจะ**นับส่วนลดซ้ำ** · แถว "ย้าย job อย่างเดียว" เป็นตัวเลขที่คำนวณตรงจากยอดใหม่จึงเชื่อได้

**5 job ที่ย้ายออก (67 ชม. · เดิม 79):** Job 10 (8) · Job 7 (12) · Job 9 (13) · Job 4 (17) · Job 5 (17) — ย้ายเป็น**คู่** (4+5 คนเดียว · 7+9 คนเดียว) จะได้ผลของการทำซ้ำเหมือนกัน
**Bank เก็บไว้ (213 ชม.):** migration 74 + สร้าง workflow 24 + Job 2 (20), 3 (12) (ALLMAP · สายข้อมูลเดียวกัน) + Job 6 (32 · STA) + Job 8 (23 · สร้างเอกสาร) + Job 8b (28 · เปิด workflow)

> ⚠️ ทางเลือก E เหลือ buffer แค่ ~3 วัน และตั้งบนสมมติฐานว่า Bank ทำได้เร็วกว่าประมาณการจริง — ถ้าสมมติฐานพลาดจะกลับไป 5.7 สัปดาห์ทันที · ถ้าอยากมั่นใจโดยไม่พึ่ง productivity ให้ย้าย Job 8 (24 ชม.) ออกอีกตัว จะเหลือ ~4.0 สัปดาห์

> **ยังไม่ตัดสิน** — แผนที่ generate อยู่ตอนนี้ยึดตามมติ (Bank ถือทั้งหมด) และเตือนไว้ใน `LLDD/md/README.md` แล้ว

---

## 1.2 🟠 ของที่อยู่ในผัง To-Be / SDD แต่ยังไม่มีในแบบของเรา

เรียงจากใหญ่ไปเล็ก — ตัวเลข **B/C** อ้างอิง [`TOBE-flow-gap-review-2026-08-19.md`](TOBE-flow-gap-review-2026-08-19.md)

> ### ✅ มติ 2026-08-24 — ไฟล์กับ IAS/MIS ย้ายจาก SFTP ไป EAI S3
> | เรื่อง | มติ | ผล |
> |---|---|---|
> | ช่องทางแลกไฟล์กับ IAS/MIS | **วางไฟล์บน EAI S3 แทน SFTP ตรง** | Job 4 อัปโหลด `AMS06001O` ขึ้น prefix ขาออก · Job 5 ดึง `AMS06001I` จาก prefix ขาเข้าแล้วย้ายไป prefix backup · **ไม่ต้องมี SFTP credential/known_hosts กับปลายทางนี้อีก** · ALLMAP และ STA **คงกลไกเดิม** |
>
> ### ✅ มติ 2026-08-24 — Job 6 ส่งค่าชดเชยไป STA ด้วย RabbitMQ แทนไฟล์ + SFTP
> | เรื่อง | มติ | ผล |
> |---|---|---|
> | ช่องทางส่งค่าชดเชยไป STA | **publish RabbitMQ message แทนไฟล์ `FRBC0001` + SFTP** | Job 6 เขียน outbox `sgi_interface_transactions` (`direction = OUT` · `status = READY`) ใน transaction เดียวกับ 10 mutation → publish exchange `sgi.interface` routing `sta.compensation.result` → `READY → SENT` (+ `outbox_status = PUBLISHED`) → **ได้ publisher confirm จึง `outbox_status = CONFIRMED` + `status = COMPLETED`** · เนื้อข้อมูลคงสัญญาเดิม **14 ฟิลด์** แต่เป็น **JSON UTF-8** (ไม่มี windows-874 แล้วเพราะไม่ใช่ไฟล์) · **ปรับตามมติข้อ 2.13 (2026-09-08): ไม่มี ACK ระดับธุรกิจ** — `outbox_status = CONFIRMED` เมื่อได้ publisher confirm จาก broker และ Job 10 เฝ้าแถวที่ยังไม่ CONFIRMED |
> | ขอบเขต transaction | **แยก commit — เลิกกติกา "SFTP ล้มเหลว = rollback ทั้ง transaction"** | ใช้ transactional outbox แบบเดียวกับ Job 4 · publish ไม่สำเร็จ = แถวค้าง `READY/FAILED_RETRY` ให้ dispatcher ส่งซ้ำ **โดยไม่ rollback การ sync สถานะ** · ส่งซ้ำปลอดภัยเพราะใช้ `message_id = sgi_interface_transactions.id` เป็น idempotency key ฝั่ง STA |
>
> ⏳ **ยังต้องยืนยันกับทีม STA/EAI ก่อนลงมือ:** ชื่อ exchange + routing key จริง (ที่ใส่ไว้เป็นค่าเสนอ) · ใครประกาศ exchange/queue และ binding · **ฟิลด์วันที่ 3/5/6 ยังต้องเป็น พ.ศ. หรือเปลี่ยนเป็น ISO ค.ศ. ได้** (ข้อจำกัด windows-874 หายไปแล้ว แต่รูปแบบวันที่เป็นสัญญาข้อมูล ไม่ใช่ข้อจำกัดของไฟล์) · STA กันรับซ้ำด้วย `message_id` ได้จริงหรือไม่ · นโยบาย DLQ/retry ของ broker · ยังต้องส่งไฟล์คู่ขนานช่วง cutover หรือไม่

> ⏳ **ยังต้องถามฝั่ง EAI ก่อนลงมือ:** ชื่อ bucket + โครง prefix (ขาออก/ขาเข้า/backup) · วิธียืนยันสิทธิ์ (IAM role ของ pod หรือ access key ใน secret) · **ใครลบอ็อบเจกต์เก่า** (lifecycle policy ของ EAI หรือ SGI ทำเอง) · IAS/MIS รู้ได้อย่างไรว่ามีไฟล์ใหม่ (poll เอง หรือมี event/notification)

> ### ✅ ปิดเพิ่ม 2026-08-24 — ตัด Job 1 ImportQSSI ออกจากขอบเขต
> | ข้อ | มติ | ผล |
> |---|---|---|
> | **DP-4** · `fcs_qssi_score` reuse ตารางเดิม 23.9M แถว หรือสร้างใหม่ | **reuse ตารางเดิมแบบอ่านอย่างเดียว** — ระบบ SBP เดิมนำเข้าให้แล้วผ่าน `POST /performance/import-qssi` | **ตัด Job 1 (ImportQSSI) ทั้ง job** · ไม่ต้อง backfill · ไม่ต้อง `SET NOT NULL` · **ไม่ต้องขอ sign-off เจ้าของ `performance.service.ts`** เพราะ SGI ไม่เขียนตารางนี้เลย |
> 
> **ผลต่อชุดส่งมอบ:** LLDD **41 → 40 ฉบับ** · Batch Job **11 → 10 entry point** · ชั่วโมง **845 → 824 → 800 → 829** (implementation 653 + unit test 153)

> ✅ **ทำไปแล้ว 2026-08-24 (7 ข้อ)** — **B5** ร้านกระทบเพิ่ม 2 ทาง (`source_system` = `ALLMAP` auto / `USER` คีย์เอง) + ปุ่ม **เพิ่มร้านกระทบเพิ่ม** ใน `k2-document.html` + กติกาเกลี่ย %ชดเชยใหม่ทั้งชุด · B4 เกณฑ์เลือกปุ่มปฏิเสธ + เหตุผล "Location ที่ไม่เหมาะสม" (`workflow.md` · `k2-document.html`) · B6 ตารางเทียบชื่อ lane ผัง ↔ section (`workflow.md`) · B7 ปลายทางบัญชี → SAP อยู่นอก workflow (`workflow.md`) · B9 `STA-SS` = `STA` ระบบเดียวกัน (`workflow.md` · `k2-flow.html`) · C4 checklist บัญชี 5 ข้อ + สาเหตุยอดไม่ตรง 2 ข้อ (`api.md`) · C5 ตัวเลข sizing/NFR 150–170 ฉบับ/เดือน (`api.md`)

| # | เรื่อง | ขนาด | ทำไมยังไม่ทำ | เริ่มได้เลยไหม |
|---|---|---|---|---|
| **B3** | **หน้างานค้างมี 2 สถานะ*ก่อน*เข้า workflow** — `รอมอบหมายงาน` (ระบบดึงครั้งแรก) · `คงค้าง` (เห็นควรไม่ชดเชย) | 🔴 **ใหญ่ · เชิงสถาปัตยกรรม** | `GET /sgi/document/tasks` อ่านจาก `getPendingFlowByUser()` ของ engine ซึ่งคืนเฉพาะงานที่**มี workflow instance แล้ว** — แต่ผังต้องการให้แสดงร้านที่ยัง**ไม่มีเอกสาร**ด้วย → ต้องมีแหล่งข้อมูลที่ 2 แล้ว merge | ⚠️ ต้องเคาะขอบเขตก่อน (กระทบทั้ง schema + API) |
| **C3** | **คำนวณชดเชยแยกตามประเภทร้าน** (`Type A,C` / `Type B,E,V(B)`) + Performance Index · Incentive Guarantee · GP ก่อน Split · ส่วนแบ่งรายได้ตามเป้า (สไลด์ 33 · 36) | 🔴 **ใหญ่** | ยังไม่เคยแตะเลย · เป็นสูตรคำนวณเงิน | ⚠️ ต้องเคาะขอบเขต — เสนอเป็นเฟส 2 |

## 1.3 🧹 หนี้ทางเทคนิคใน prototype

| เรื่อง | รายละเอียด | ผลถ้าไม่ทำ |
|---|---|---|
| **dead code ใน `assets/sbp.js`** | โหมด `data-nav="application"` (บรรทัด ~160–170) + `switchStep()` (~386) ยังลิงก์ไป `recruitment.html` · `application.html` · `application-step1/3.html` ซึ่ง**ถูกลบไปหมดแล้ว** | ไม่พังตอนนี้ แต่คนอ่านโค้ดจะเข้าใจผิดว่ามีหน้าเหล่านั้น · CLAUDE.md เขียนกำกับไว้ว่า *"Do not use or copy that mode"* |
| **หน้า reference ที่ถอดจาก sidebar** | `k2-permissions.html` · `k2-operators.html` เก็บไว้อ้างอิงแต่ไม่มีทางเข้า | ตั้งใจ — RBAC ใช้ของระบบ SBP เดิม |

---

# ส่วนที่ 2 · เรื่องที่ยังต้องตัดสินใจ (39 ข้อ)

**แบ่ง 4 กลุ่มตามความเร่งด่วน** — ต้องตัดสินก่อนเขียนโค้ด **1** · กระทบ schema/สัญญา **30** · ตัดสินทีหลังได้ **3** · ต้องไปถามทีมอื่น **6**

## 🔴 กลุ่ม 1 — ต้องตัดสินก่อนเริ่มเขียนโค้ด (1 ข้อ)

### 1.1 · DP-11 — ตัวเลข "เงินประกันรายได้" SGI เป็นต้นทาง หรือ `fr_store_insure` ยังคีย์มือ ?
| | |
|---|---|
| **ทางเลือก** | (ก) SGI เขียนตัวเลขลง `fr_store_insure` เอง · (ข) ทีมงานยังคีย์มือเหมือนเดิม |
| **บล็อกอะไร** | **ไม่ได้บล็อกการสร้างตาราง** — DDL ของ `sgi_compensation_histories` นิ่งแล้วและอยู่ใน `output/sql/sgi_schema.sql` (ปรับถ้อยคำ 2026-09-09 · เดิมเขียนว่า "สร้างไม่ได้" ซึ่งขัดกับสคริปต์ติดตั้งที่สร้างตารางนี้จริง) · สิ่งที่ยังตัดสินไม่ได้คือ **ใครเป็นเจ้าของตัวเลขและใครเขียนลงตารางนี้** → กระทบ **Job 6** (ยอดที่ส่งไป STA) และกระทบว่าต้อง sync กับ `fr_store_insure` ของระบบเดิมหรือไม่ · **ลง schema ได้เลย แต่ยัง implement ตัวเขียนข้อมูลไม่ได้** |
| **ประเภท** | **ธุรกิจล้วน** ไม่ใช่เทคนิค |
| **⚠️ ไม่ใช่เรื่องเดียวกับเกณฑ์ 100,000** | `100,000` = **วงเงินอนุมัติ** (ใครเซ็น) ปิดไปแล้ว 2026-08-18 · **DP-11 = ตัวเลขเงินที่อนุมัติแล้วไปเก็บที่ไหน** · ระบบเดิมมี `sps_store.fr_store_insure` (708 แถว · `store_id`·`year`·`month`·`money_support`·`split` ต่อ `order_id`) ที่โมดูล inquiry เขียนอยู่แล้ว และ**มิติซ้ำ**กับ `sgi_compensation_histories` ที่เราจะสร้าง |
| **ต้องถามฝั่งบัญชี/SBP ก่อนตอบ** | `fr_store_insure` วันนี้ **ใครกรอก · กรอกจากอะไร · เป็นตัวเลขเดียวกับเงินชดเชยที่ผ่าน workflow เราไหม** |
| **ใครตอบ** | เจ้าของกระบวนการฝั่งบัญชี/SBP |

---

## 🟠 กลุ่ม 2 — ตัดสินได้ระหว่างทาง แต่กระทบ schema / สัญญา API (43 ข้อ · เปิดอยู่ 30 · ปิดแล้ว 13)

| # | เรื่อง | ทางเลือก | กระทบ |
|---|---|---|---|
| **2.1** | **DP-13** — `BranchTypeFGIName` ของ `Type V(C)` และ `Type V(B)` <br><small>(นิยามอยู่ใน `database.md` · ไม่ใช่ทะเบียน DP-1…DP-12 ของ `SBP/`)</small> | คงเป็น `C` ทั้งคู่ · หรือได้รหัสใหม่คนละตัว | ตัวกรองประเภทร้านในหน้ารายงาน (ตอนนี้ 7 ค่า) · master mapping · **SDD สไลด์ 36 จัดกลุ่มสูตรคำนวณเป็น `Type B, E, V(B)` ซึ่งบ่งว่า V(B) ไม่ใช่ C** |
| **2.2** 🟠 **มีคำตอบฝั่งข้อมูลแล้ว เหลือเคาะชื่อแสดงผล** | **DP-13b** — ชื่อแสดงผลของ `E` (= B(2)) <br><small>(พบ 2026-08-24 ตอนเติม mock data ประเภทร้าน)</small> | รายการชื่อแสดงผลเป็น **10 ชนิด** (เพิ่ม `SBP Type E`) · หรือ `E` ใช้ชื่อแสดงผลอื่นที่มีอยู่แล้ว | `E` มีจริงใน master `BranchTypeFGIName` (7 ค่า `A B C D E PTT บริษัท`) และมีในตัวกรองหน้ารายการ/รายงาน แต่ **ไม่อยู่ในรายการชื่อแสดงผล 9 ชนิด** ของ `database.md` · prototype ใช้ `SBP Type E` ไปพลางก่อน <br>✅ **master จริงตอบแล้ว 2026-09-16** (`docs/ข้อมูล Master K2.xlsx` → `docs/K2-master-data.md`): `BranchTypeProfile` มี **8 แถว** และ `E` คือ **`BranchTypeFGIName` ของ `BranchTypeCode = 2` ซึ่งชื่อฝั่ง MM คือ `B(2)` และฝั่ง FMS คือ `FB2`** → ยืนยันว่า `E` = `B(2)` จริง ไม่ใช่ประเภทร้านชนิดใหม่ <br>🔴 **กับดักที่เพิ่งเห็น:** ชื่อฝั่ง FGI **ซ้ำกัน** — `BranchTypeCode` `3` และ `9` เป็น `C` ทั้งคู่ · และ `0` เป็น `บริษัท` (ไทยปนอังกฤษ) → **แปลงกลับจากชื่อ FGI เป็นรหัสแบบ 1:1 ไม่ได้** ทุกที่ที่เก็บ/เทียบต้องใช้ **รหัส** ไม่ใช่ชื่อ · `${branchTypeFGIName}` ในอีเมลเดิมก็ใช้ชื่อชุดนี้ |
| **2.3** ✅ **ปิดแล้ว 2026-09-02** | **คอลัมน์ที่วิเคราะห์แล้วว่าต้องมี แต่ยังไม่ได้เติมลง DDL** <br><small>(พบ 2026-08-24 ตอนไล่ `table.column` ทุกไฟล์)</small> | — | `sgi_compensation_documents.approver_snapshot` (JSONB) อยู่ใน `CREATE TABLE` แล้ว · `sgi_impacted_stores.transfer_sbp_date` (DATE · nullable) เติมลง DDL เมื่อ 2026-09-02 ให้ตรงกับ mapping ใน `database.md` และ entity ที่เอกสาร LLDD 3 ฉบับ map ไว้อยู่แล้ว |
| **2.14** 🔴 **ต้องเคาะก่อนเขียน Job 2/8** | **`allmap_url` มีแต่คนอ่าน ไม่มีใครเขียน** <br><small>(พบ 2026-09-09 ตอนไล่โค้ด Job 2 เทียบกับ DB ใหม่)</small> | **ข้อเท็จจริง:** ALLMAP ส่ง `URL_EMBEDDED` มาตั้งแต่ Job 2 · `sgi_compensation_documents.allmap_url` มีคอลัมน์อยู่ และ `GET /sgi/document/{docNo}` คืน `allmapUrl` · Job 7/9/12 ก็ SELECT คอลัมน์นี้ · หน้า `k2-document.html` มีการ์ด "แผนที่ (POI จากระบบ AllMap)" กับปุ่มเปิด ALLMAP <br>**แต่ `INSERT INTO sgi_compensation_documents` ของ Job 8 ไม่มี `allmap_url` และไม่มี API ไหนเขียนเลย** · `sgi_fgi_impact_stores` ก็ไม่มีคอลัมน์เก็บ → **ค่าหายระหว่าง Job 2 กับ Job 8** <br>**ทางเลือก ก.** เพิ่ม `allmap_url` ใน `sgi_fgi_impact_stores` ให้ Job 2 เขียน แล้ว Job 8 คัดลอกขึ้นเอกสาร · **ข.** ให้ Job 8 re-query ALLMAP (เพิ่ม dependency ที่ตอนนี้ไม่มี) · **ค.** ประกอบ URL จากพิกัด (ทำไม่ได้ตอนนี้ — ยังไม่มีใครเติม lat/lng) · **ง.** ตัดฟีเจอร์แผนที่ทิ้ง (ต้องขอ business sign-off) | กระทบ **Job 2 · Job 8 · หน้าจอเอกสาร** · ถ้าเลือก ก. ควรตัดสินพร้อมเรื่อง `branchtype` snapshot ที่เป็นปัญหาชนิดเดียวกัน (ดู `batchjob/JOB-02-ImportImpactStore-อธิบายละเอียด.md` หัวข้อ 7 และ 9) |
| **2.15** 🟠 **ปิดครึ่งหนึ่ง 2026-09-12** | **แถวที่ตัดสินไม่ได้ค้างเป็น `W` — ยังไม่มีนโยบายรองรับ** <br><small>(เดิมเชื่อว่าสาเหตุคือ master ไม่ครบ · profile ฐานจริงแล้วพบว่าคนละเรื่อง)</small> | ✅ **ส่วนที่ปิดแล้ว — แหล่งของ `branchtype`:** วัดจากข้อมูลจริง 23,120 แถวของ `CREATE_BY='ALM'` เปลี่ยนจาก snapshot มาใช้ `mas_store.status_type` ปัจจุบัน **ผลตัดสินพลิกแค่ 19 แถว (0.08%)** และฝั่งร้านใหม่ (`branchtype_n='F'`) **พลิก 0 แถว** → **ไม่ต้องเพิ่มคอลัมน์ snapshot** ใช้ join master ต่อไปได้ <br>✅ **สาเหตุที่เคยเดาไว้ผิด:** จาก 3,064 แถวที่ค้าง `W` จริง — **W1 (master ไม่ครบ) = 0 แถว** · W2 = 0 · W4 = 0 · **W3 (ไม่มีสัญญา SBP คลุมงวด) = 2,684 แถว (87.6%)** · **W5 ใหม่: `FPT1` ที่ `cancel_type` ไม่ใช่ `'06'` = 382 แถว (12.5%)** <br>🔴 **ส่วนที่ยังค้าง:** ร้านที่**ไม่มีสัญญา SBP** ควรเป็น `N` ทันที หรือค้าง `W` รอสัญญาเข้ามาทีหลัง — ทั้งกฎ DENY N4 และ PASS P1④ อ้าง `sbp_start_date_i`/`sbp_cancel_date_i` เมื่อเป็น NULL ทั้งคู่ให้ผล NULL จึงไม่มีคำสั่งไหนแตะ · **นี่คือช่องว่างของกติกา ไม่ใช่บั๊กของการ join** | โค้ด Job 2 ทำ **ทางเลือก ข.** ไว้แล้ว (คง `W` + metric `remainingWCount` + alert เมื่อ > 0) · ต้องเคาะเฉพาะนโยบาย W3/W5 · ตัวเลขทั้งหมดจาก `output/legacy-oracle/data.json` |
| **2.17** ✅ **ปิดแล้ว 2026-09-12** | ~~`sgi_fgi_impact_processes.process_status` ไม่มีชุดค่าที่นิยามไว้~~ → **วงจร 5 สถานะ** | **มติ: `IMPORTED` → `SALES_READY` → `READY_DOCUMENT` → `DOCUMENT_CREATED` → `CLOSED`** พร้อม `CHECK` และ **`DEFAULT 'IMPORTED'`** <br>**ใครเลื่อนที่จุดไหน** (ไล่จากสเปกจริงของแต่ละ job + กราฟ `JOB_DEPENDENCIES`): `IMPORTED` = **Job 2** สร้างแถวแม่ตอนนำเข้า · `SALES_READY` = **Job 5** ได้ยอดขายและคำนวณ `growth_rate_diff` เสร็จ · `READY_DOCUMENT` = **Job 6** ตั้งรอบชดเชยเรียบร้อย (Job 8 อ่านค่านี้หยิบงาน) · `DOCUMENT_CREATED` = **Job 8** สร้างเอกสารสำเร็จ · `CLOSED` = **Job 6** ปิดรอบ (`flag_action` Y→N) <br>⚠️ **Job 4 และ Job 8b ไม่เลื่อนสถานะนี้** — การขอยอดขายอยู่ที่ `sales_request_status` ผลการเปิด workflow อยู่ที่ `workflow_generation_status` <br>⚠️ เข้าเงื่อนไขทั้ง `READY_DOCUMENT` และ `CLOSED` ในรันเดียว → **`CLOSED` ชนะ** (terminal) <br>⚠️ แถวที่คู่ร้านถูกตัดทิ้ง (`verify_status='N'`) หรือค้าง `W` **คงเป็น `IMPORTED` ตลอดไป** — `IMPORTED` แปลว่า “ยังไม่มีอะไรเกิดขึ้น” ไม่ใช่ “กำลังดำเนินการ” <br>**กติกาอนุมานตอน migrate** (ข้อมูลจริง 24,783 แถว): `CLOSED` 7,542 · `DOCUMENT_CREATED` 572 · `READY_DOCUMENT` 0 · `SALES_READY` 2,498 · `IMPORTED` 14,180 — **ห้ามใส่ `IMPORTED` ให้ทุกแถว** เพราะ 8,114 แถวมีความคืบหน้าไปแล้วจริง | แก้แล้ว: DDL + `CHECK` + `DEFAULT` · `output/sql/sgi_schema.sql` · `LLDD-Database` · `LLDD-BE-Data-Migration-Cutover` ข้อ 5.2.3 (ตารางอนุมาน) · `batchjob/JOB-02` · โค้ด Job 2 · 🔴 **ข้อ 2.31 (`status_code` ของเอกสาร) ยังค้าง — เป็นปัญหาชนิดเดียวกัน ควรเคาะต่อ** |
| **2.18** 🔴 **ต้องเคาะก่อนเขียนโค้ด** | **ขอบเขต transaction ของการเขียน 3 ตาราง** <br><small>(พบ 2026-09-09 ตอนเทียบสัญญา transaction ในเอกสารกับ SQL จริง)</small> | **ข้อเท็จจริง:** FK บังคับลำดับ `sgi_impacted_stores` → `sgi_fgi_impact_processes` → `sgi_fgi_impact_stores` · ถ้า commit แยกกันแล้วพังกลางทาง จะเหลือ **แถวแม่ที่ไม่มีลูก** ค้างในระบบ และรอบหน้า `ON CONFLICT DO NOTHING` จะไม่แก้ให้ · ระบบเดิมยิ่งแย่กว่า — **ซ้อน `TransactionTemplate` กับ `getTransaction()/commit()` อีกชั้น** (`ImpactStoreService.java:104`) จน transaction ชั้นนอกคุมอะไรไม่ได้ (ดูหัวข้อ 11 · L5) <br>**ต้องระบุให้ชัด:** ขอบเขตเป็น **ต่อ candidate หนึ่งราย** หรือ **ต่อ chunk** · chunk ล้มแล้ว rollback แค่ chunk หรือทั้ง job · การเปลี่ยน `verify_status` (ขั้น ⑤) อยู่ใน transaction เดียวกับการ insert หรือแยก | กระทบ **Job 2 · เคสทดสอบกลุ่ม 6** · **ห้ามซ้อน transaction สองชั้นตามระบบเดิม** ไม่ว่าเลือกทางไหน · สัญญาที่เขียนไว้ตอนนี้คือ "ทั้ง 3 ขั้นของ candidate หนึ่งรายอยู่ใน transaction เดียวกัน · chunk fail rollback เฉพาะ chunk" — ต้องได้รับการยืนยัน |
| **2.19** 🔴 **ต้องเคาะก่อนเขียนโค้ด** | **การกรองรัศมี 1/2 กม. — requirement ใหม่หรือของเกิน** <br><small>(พบ 2026-09-09 ตอนเทียบคิวรี candidate ใน LLDD กับ `ImportStoreJdbc.selectVsevenImpact`)</small> | **ข้อเท็จจริง:** ระบบเดิม **ไม่ได้กรองระยะทางในคิวรีเลย** — ดึงทุกแถวที่วิว `SEVEN_IMPACT_VIEW` ส่งมา เพราะถือว่า **ALLMAP คัดร้านที่อยู่ในรัศมีมาให้แล้ว** · แต่คิวรีใน LLDD เพิ่ม `distance_km <= CASE WHEN กทม./ปริมณฑล THEN 1 ELSE 2 END` เข้ามา <br>**ถ้าเก็บไว้** = กรองสองชั้น · ผลอาจน้อยกว่าระบบเดิมถ้านิยามรัศมีสองฝั่งไม่ตรงกัน (และต้องแปลงหน่วยตาม `DISTANCE_UNIT` ก่อนเทียบ) · **ถ้าเอาออก** = เชื่อ ALLMAP เหมือนเดิม | กระทบ **จำนวนคู่ร้านที่เข้าระบบทั้งงวด** · ถ้าจะเก็บไว้ **ต้องขอ business sign-off** ไม่ใช่ตัดสินในเอกสารเทคนิค · เกี่ยวโยงกับกติกาแปลงหน่วยระยะทาง (ดู `batchjob/JOB-02-ImportImpactStore-อธิบายละเอียด.md` หัวข้อ 4) |
| **2.20** ✅ **ปิดแล้ว 2026-09-13** | ~~ร้านที่ไม่มีแถวแม่ จะเก็บข้อมูลคู่แข่งไม่ได้~~ → **ข้ามแถวนั้น (ทางเลือก ก.)** | **มติ: Job 3 ข้ามคู่แข่งของร้านที่ไม่มีแถวแม่ของงวดนั้น แล้วนับ `skippedNoParentCount` + alert** <br>**หลักฐานจากโค้ดเดิม:** ทั้งระบบมี**ผู้อ่าน `FGI_IMPACT_COMPETITOR` รายเดียว** คือคิวรีส่งออกที่ `ExportJdbc.java:1605-1628` ซึ่งเป็น **`INNER JOIN fgi_impact_store_on_process`** (บวก `INNER JOIN fgi_impact_store_compensate`) → **คู่แข่งของร้านที่ไม่มีแถวแม่ ระบบเดิมก็ไม่เคยเอาไปใช้เลย** <br>**หลักฐานจากข้อมูลจริง** (`FGI_IMPACT_COMPETITOR_BK_20250515` 228,116 แถว · 2017-2025): แถวที่ไม่มีแถวคู่ร้าน **16,796 แถว** ผูกกับรอบชดเชยได้เพียง **14 แถว (0.08%)** ส่วนแถวที่มีแถวคู่ร้าน 211,320 แถว ผูกได้ 47,045 แถว (22.3%) <br>→ **16,782 จาก 16,796 แถว (99.92%) เป็นข้อมูลตายอยู่แล้ว** การข้ามจึงไม่ใช่การสูญเสียเชิงหน้าที่ <br>⚠️ **ความเสี่ยงที่เหลือวัดได้แล้ว:** 14 แถวจาก **ร้านแค่ 4 ร้าน** (`07459` 2 งวด · `09257` 1 งวด · `00976` 3 งวด) ตลอด 8 ปี = **0.006%** ของข้อมูลทั้งหมด · และแม้แต่ 14 แถวนั้นระบบใหม่ก็ข้ามเหมือนกัน เพราะ Job 2 สร้างแถวแม่จากคู่ร้าน ซึ่งร้านเหล่านั้นไม่มีคู่ร้านในงวดนั้น | โค้ด Job 3 ทำไว้แล้ว (`SGI_JOB3_MISSING_PARENT=skip` · นับ + log ตัวอย่างรหัสร้าน + alert) · สลับเป็น `fail` ได้ด้วย env ถ้าภายหลังต้องการบังคับให้ Job 2 รันครบก่อน · **ไม่ต้องแก้ DDL และไม่ต้องเพิ่มคอลัมน์ `impacted_store_code`** |
| **2.21** ✅ **ปิดแล้ว 2026-09-09** | **คู่แข่งยี่ห้อเดียวกันหลายสาขา เก็บได้แค่สาขาเดียว** | **แก้ DDL แล้ว** — เดิม `sgi_fgi_impact_competitors` มีคอลัมน์เดียว `competitor_code` ที่ FK ไป master แบรนด์ และ UNIQUE คีย์ด้วยแบรนด์ · **พิสูจน์จากไฟล์จริง `docs/ตัวอย่างไฟล์ที่วางให้ K2 เอาเข้าระบบ/20170609_200402_BPM06003O_.txt`** (1,312 แถว): ร้าน `07109` มีแฟมิลี่มาร์ท **80 สาขา** ใกล้ร้าน · ถ้าคงคีย์เดิมจะ **เหลือ 165 จาก 1,312 แถว — หายไป 87%** | แยกเป็น **`competitor_store_code`** (รหัสสาขาจาก ALLMAP · ไม่มี FK · nullable) และ **`brand_code`** (FK ไป master · nullable) · UNIQUE เป็น `(impact_process_id, competitor_store_code, period_key)` · แก้ `sgi_document_competitors` แบบเดียวกันแล้ว (Job 7 คัดลอกตรง ๆ) |
| **2.22** ✅ **ปิดแล้ว 2026-09-09** | **`COMPET_ID` จาก ALLMAP ตรงกับ master 11 รหัสหรือไม่** | **ตอบแล้ว: ไม่ตรง และไม่ใช่ของระดับเดียวกัน** — ไฟล์จริงมี `COMPET_ID` **222 ค่าไม่ซ้ำ** (`4832` · `789` · `TD58_08` · `LS3550`) จาก **7 แบรนด์** · เป็น **รหัสสาขา** ไม่ใช่รหัสแบรนด์ · และ **56 จาก 1,312 แถวส่ง `COMPET_ID` ว่างมา** | เก็บลง `competitor_store_code` **ที่ไม่มี FK** · แบรนด์ได้จากการ map ชื่อ (`NAMT`/`NAME`) เข้า master แล้วใส่ `brand_code` (nullable · map ไม่ได้ = เก็บแถวไว้ + นับ `unmappedBrandCount` + alert) · แถวที่รหัสสาขาว่างต้องแปลงเป็น **NULL** (PostgreSQL ถือว่า NULL แต่ละตัวต่างกันใน UNIQUE จึงไม่ชนกันเอง) |
| **2.23** ✅ **ปิดแล้ว 2026-09-13** | ~~สัญญา idempotency ของ Job 3 — งวดที่มีข้อมูลค้างไม่ครบ~~ → **คงพฤติกรรมเดิม (ข้ามทั้งงวด) + เทียบจำนวนกับต้นทางแล้ว alert** (ทางเลือก ก.) | **หลักฐานชี้ขาด:** ถ้าเคยนำเข้าไม่ครบแล้วมีคนมาเติมทีหลัง งวดนั้นต้องมี `create_date` มากกว่า 1 วัน (เพราะกติกา "ข้ามทั้งงวด" บล็อกการรันซ้ำ การเติมต้องทำด้วยมือ) — ผลจริง: **88 งวดจาก 88 งวด insert จบภายในวันเดียว · งวดที่ insert หลายวัน = 0** <br>**6 งวดที่ดูผิดปกติ อธิบายได้หมด ไม่ใช่การล้มกลางคัน:** `2020-05` (15 แถว/9 ร้าน) และ `2022-05` (4 แถว/3 ร้าน) มี timestamp เดียว = รันครั้งเดียวจบ เดือนที่ร้านถูกกระทบน้อยจริง · `2024-07` และ `2025-01..04` คือช่วงที่ **ทั้งระบบหยุดทำงาน** (คู่ร้านงวด 2024-07 ก็เหลือ 1 แถว เทียบกับ 2024-06 ที่มี 286 แถว) และแถวปี 2025 ถูก insert ตั้งแต่ `2024-04-07` = ข้อมูลล่วงหน้า จากการรันครั้งเดียว <br>**ยืนยันจากโค้ด:** ไม่มีคำสั่ง `DELETE FROM FGI_IMPACT_COMPETITOR` ที่ใดเลยทั้ง repo — ทางเลือก (ข) "ล้างงวดแล้วนำเข้าใหม่" เป็นของใหม่ล้วน และข้อมูล 8 ปีบอกว่าไม่จำเป็น | โค้ด Job 3 ทำไว้แล้ว (`SGI_JOB3_ON_EXISTING=skip`) · **เพิ่มของที่ระบบเดิมไม่มี**: เทียบ `existingRowCount` กับ `sourceCount` ทุกรอบ แล้ว log ระดับ error + ส่งเมลเมื่อของเดิมน้อยกว่าต้นทาง (ปิดกับดัก L7 ที่ระบบเดิมรายงานว่าสำเร็จทั้งที่ข้อมูลขาด) · สลับเป็น `upsert` ได้ด้วย env |
| **2.24** 🔴 **ต้องเคาะก่อนเขียน Job 4** | **แถว `STA` ไม่มีวันได้ยอดขายจาก IAS** <br><small>(พบ 2026-09-09 ตอนไล่โค้ด Job 4)</small> | **ข้อเท็จจริง:** `insertImpactStoreSales` (`ImportStoreJdbc.java:436`) กรอง `CREATE_BY = 'ALM'` จึง **ไม่สร้างหัวสรุปยอดขายให้แถว `STA` เลย** · แต่ Job 2 กฎ **P2 ปล่อยแถว `STA` ผ่านเป็น `P` โดยไม่ตรวจอะไร** → แถวเหล่านั้นค้างเป็น `P` ตลอดไป ไม่มี `growth_rate_diff` → **Job 8b เปิด workflow ไม่ได้** <br>**ทางเลือก ก.** เป็นพฤติกรรมที่ตั้งใจ — ต้องระบุว่าเคส STA ได้ยอดขายจากเส้นทางไหนแทน · **ข.** เป็นช่องโหว่ — ต้องให้ Job 4 รับแถว `STA` ด้วย · **ค.** เคส STA ไม่ต้องชดเชยผ่าน flow นี้เลย (ต้องยืนยันกับ business) | กระทบ **Job 2 · Job 4 · Job 8b** · ผูกกับข้อค้าง P2/STA ของ Job 2 โดยตรง (ดู `batchjob/JOB-04-PrepareImpactStoreToIAS-อธิบายละเอียด.md` หัวข้อ 5) |
| **2.25** 🔴 **ต้องเคาะก่อนเขียน Job 4** | **`open_date` ของทั้งสองร้าน ไม่มีที่เก็บในตารางใหม่** <br><small>(พบ 2026-09-09 ตอนเทียบ SQL ของ Job 4 กับ DDL ใหม่)</small> | **ข้อเท็จจริง:** ระบบเดิมเก็บ `OPENDATE_I`/`OPENDATE_N` ไว้บน `FGI_IMPACT_STORE_SALES` เอง เพราะ **ทั้งกติกาคัดร้าน (T1: ร้านเก่าเปิดก่อน 12 เดือน 15 วัน · T2: ผ่านมาแล้ว 16 วันนับจากร้านใหม่เปิด) และฟิลด์ที่ 2 ของไฟล์ AMS06001O ใช้ค่าเหล่านี้** · **`sgi_fgi_impact_sales_summaries` ไม่มีทั้งสองคอลัมน์** <br>**ทางเลือก ก.** เพิ่ม 2 คอลัมน์เป็น snapshot ณ เวลาสร้างหัวสรุป · **ข.** join `mas_store.open_date` ทุกครั้งตอนคัดร้าน <br>**เรื่องที่ต้องเคาะพร้อมกัน:** เงื่อนไข **S4** ของระบบเดิม (ไม่สร้างหัวสรุปให้ร้านที่มีรอบชดเชย `flag_action = 'Y'` อยู่แล้ว) ยังต้องมีไหม — ตารางใหม่ผูกหัวสรุปกับ `impact_process_id` ด้วย `UNIQUE` ทำให้ความสัมพันธ์กลับด้าน | กระทบ **DDL · Job 4 · Job 5** · ถ้าเลือก ข. แล้ว master ถูกแก้ย้อนหลัง **ร้านที่เคยผ่าน T1/T2 อาจกลายเป็นไม่ผ่าน** และ **ตรวจย้อนหลังไม่ได้** ว่าตอนส่งใช้ค่าอะไร — เป็นปัญหาชนิดเดียวกับ `branchtype` (ข้อ 2.15) |
| **2.26** 🔴 **ต้องขอ business sign-off** | **`growth_rate_diff` เป็น NULL แล้วระบบตีความว่า "เข้าเกณฑ์ชดเชย"** <br><small>(พบ 2026-09-09 ตอนไล่โค้ด Job 5)</small> | **ข้อเท็จจริง:** `updateStatusImpactStoreSaleByDiff` (`ImportJdbc.java`) ใช้ `CASE WHEN NVL(GROWTH_RATE_DIFF, 0-1) < 0 THEN 'Y' ELSE 'N' END` → **`NULL` ถูกแทนด้วย `-1` จึงได้ `Y`** · `NULL` เกิดเมื่อ **ยอดขายเฉลี่ยปีที่แล้วเป็น 0** (สูตรใช้ `DECODE(ตัวหาร, 0, NULL, ตัวหาร)` เพื่อเลี่ยงหารด้วยศูนย์) แปลว่า **"คำนวณไม่ได้" ไม่ใช่ "ยอดตก"** · ร้านนั้นจะไหลเข้า workflow ให้คนอนุมัติจ่ายเงินโดยไม่มีธงบอกว่าตัวเลขเชื่อไม่ได้ <br>**ทางเลือก ก.** คงพฤติกรรมเดิม (ให้ประโยชน์แก่ร้าน) **แต่เพิ่มธง/หมายเหตุ** ว่าคำนวณไม่ได้ · **ข.** ใช้ `sales_status = 'E'` ที่ DDL รองรับอยู่แล้ว ให้คนตรวจก่อน · **ค.** ให้เป็น `N` | 🔴 **กระทบว่าจะจ่ายเงินหรือไม่จ่าย** — ต้องมี business sign-off · ยิ่งอันตรายเมื่อรวมกับ **L7** (DAO กลืน exception แล้วคืน 0) เพราะขั้นคำนวณล้มเงียบก็ให้ผลเป็น NULL เหมือนกัน → ได้ `Y` ทั้งที่ระบบไม่เคยคำนวณเลย (ดู `batchjob/JOB-05-ImportImpactSaleFromIAS-อธิบายละเอียด.md` หัวข้อ 6 · 12) |
| **2.27** 🔴 **ต้องเคาะก่อนเขียน Job 5** | **โครง `sgi_sales_transactions` เปลี่ยนจาก "แถวกว้าง" เป็น "แถวแคบ" — สูตรคำนวณย้ายมาตรง ๆ ไม่ได้** <br><small>(พบ 2026-09-09 ตอนเทียบ SQL ของ Job 5 กับ DDL ใหม่)</small> | **ข้อเท็จจริง:** ระบบเดิมเก็บ 1 แถว = 1 วัน × **4 คู่คอลัมน์** (`SALES_BEFORE_IMPACT_PAST`/`PRESENT` · `SALES_AFTER_IMPACT_PAST`/`PRESENT`) สูตร `AVG` จึงเขียนใน `SELECT` เดียวได้ · **ตารางใหม่เก็บ 1 แถว = 1 วัน × 1 หน้าต่าง** โดยมี `window_no` (1–4) → **ต้องเขียนสูตรใหม่เป็น pivot ทั้งหมด** <br>**เรื่องที่ต้องเคาะพร้อมกัน:** **(1)** `window_no` เลขไหนคือหน้าต่างไหน (DDL มีแค่ `CHECK BETWEEN 1 AND 4`) · **(2)** `sales_diff` ลบกับอะไร · **(3)** `is_outlier` เกณฑ์ `\|sales_diff\| ≥ 50` เป็นหน่วยอะไร (บาท? %?) · **(4)** `source_checksum` เป็น **`NOT NULL` ไม่มี DEFAULT** ต้องนิยามว่าเป็น checksum ของไฟล์หรือของแถว · **(5)** `TOTAL_WORKING_DAY != 60` ของโค้ดเดิม vs "วันทำการ < 60" ที่ผังเขียน — **ไม่ตรงกัน** | กระทบ **Job 5 · Job 8b · หน้าจอ** · **ต้องพิสูจน์ด้วยข้อมูลจริงว่าสูตรใหม่ให้ผลเท่าสูตรเดิม** ก่อนขึ้นใช้งาน — ผิดแปลว่าจ่ายเงินผิด · insert ไม่ผ่านเลยถ้าไม่มีนิยาม `source_checksum` |
| **2.28** 🔴 **ต้องเคาะก่อนเขียน Job 6** | **argument `yyyyMMdd` ตัวเดียวคุมพฤติกรรมสามอย่างพร้อมกัน** <br><small>(พบ 2026-09-09 ตอนไล่โค้ด Job 6)</small> | **ข้อเท็จจริง:** `ExportController.getImpactStoreToFS` แตกวันที่อ้างอิงเป็น **(1)** `INITDATE` = งวดที่ใช้ซิงก์สถานะ **(2)** `month`/`year` = **เดือนก่อนหน้า** ที่ใช้ตรวจคะแนน QSSI **(3)** `curDate` = วันของเดือน ที่เป็นตัวตัดสิน Gate 1 (`>= dateStartInitToSTA` = 7) · **การรันย้อนหลังจึงเปลี่ยนทั้งสามอย่างพร้อมกัน** ไม่ใช่แค่ "รันงวดนั้นใหม่" <br>**ทางเลือก ก.** แยกเป็น 3 พารามิเตอร์ใน JSON input · **ข.** คงตัวเดียวแต่เขียนสัญญาให้ชัดว่ากระทบอะไรบ้าง <br>**เรื่องที่ต้องเคาะพร้อมกัน:** ค่าคงที่ธุรกิจ 3 ตัว (`dateStartInitToSTA` = 7 · `numWaitPay` = 3 · `categoryQssi` = 8,9,12,1,10,16) อยู่ใน `ApplicationResources.properties` ของระบบเดิม — ระบบใหม่ตกลงว่าค่าธุรกิจอยู่ที่ `mas_param`/`common_code` **ห้าม hardcode** | กระทบ **การรันซ่อมย้อนหลัง** ซึ่งเป็นงาน ops ที่ต้องทำจริง · และ **argument ผิดรูปแบบตอนนี้ล้มเงียบ** (catch ตั้งแค่ errorMsg ไม่มี exit code — ดู `batchjob/JOB-06-ExportImpactStoreToFS-อธิบายละเอียด.md` หัวข้อ 3 · 13 · L3) |
| **2.29** 🔴 **ต้องเคาะก่อนเขียน Job 6** | **การผูกคู่ `I` ↔ `N` และงวด Statement ยังไม่มีที่เก็บ** <br><small>(พบ 2026-09-09 ตอนเทียบ 9 mutation ของ Job 6 กับ DDL ใหม่)</small> | **ข้อเท็จจริง:** ทุกแถวที่ส่ง STA ส่งเป็น **คู่** — ฝั่งร้านถูกกระทบ (`COMPENSATE_*_I`) และฝั่งร้านเปิดใหม่ (`COMPENSATE_*_N`) · ระบบเดิมใช้ **`compensate_i_id`** บน `FGI_IMPACT_STORE_COMPENSATE` เป็นตัวเชื่อมสองฝั่ง · **ตารางใหม่ยังไม่ระบุว่าผูกอย่างไร** <br>**และ `STMT_MONTH`/`STMT_YEAR` (ฟิลด์ 6 ของสัญญา 14 ฟิลด์) ยังไม่ระบุว่าเก็บที่คอลัมน์ไหน และใครเป็นคนเติม** — ฟิลด์นี้ว่างได้ แต่ต้องรู้ว่าเมื่อไรควรมีค่า | กระทบ **DDL · Job 6 · Job 11** (ที่รับสถานะจ่ายกลับ) · ผูกกับ `DECISIONS` ข้อ **2.4 (DP-6)** ที่ยังไม่ปิดเรื่องการออกแบบ `sgi_interface_transactions`/ตารางค่าชดเชย |
| **2.30** 🔴 **ต้องเคาะก่อนเขียน Job 7** | **สถานะ "รอ sync" ไม่มีที่เก็บ · `source_system` ใช้คนละค่า · ไม่มี `source_row_id`** <br><small>(พบ 2026-09-10 ตอนไล่โค้ด Job 7)</small> | **(1) สถานะรอ sync:** ผังระบุว่าถ้ายังไม่มี `sgi_compensation_documents` ให้ "คงสถานะรอ sync / log pending" แต่ **ไม่มีคอลัมน์ไหนเก็บสถานะนี้** · ระบบเดิมไม่มีปัญหาเพราะผูกด้วย `STORECODE_I` ไม่ต้องรอเอกสาร — ถ้าเก็บแค่ใน log จะไม่มีทางรู้ว่ามีกี่แถวค้างอยู่ <br>**(2) `source_system`:** สเปก SQL ใช้ `'ALLMAP'` แต่ผังใน `job-batch.html` เขียน `ALM` — **เป็นเงื่อนไขของ `DELETE` ตอน prune** ใช้ผิดค่าจะลบไม่ตรงกลุ่ม หรือ **ลบแถวที่ผู้ใช้คีย์เอง** <br>**(3) `source_row_id`:** ฟิลด์ 1 ของไฟล์ `BPM06003O` เดิมคือ PK ของแถวต้นทาง (`IMPACT_COMPETITOR_ID`) · ตารางใหม่ไม่มีที่เก็บ → **ตามรอยกลับไม่ได้ว่าข้อมูลบนเอกสารมาจากแถวไหน** <br>**(4) gate ข้อมูลผู้อนุมัติ:** ระบบเดิมคัดรอบที่ `dv_*`/`gm_*`/`avp_*` ไม่ครบออก (`getErrorProcess`) · ระบบใหม่ย้าย gate นี้ไป Job 8b แล้ว — ต้องเคาะว่ายังต้องมีที่ Job 7 อีกไหม | กระทบ **DDL · Job 7 · Job 8b** · การ prune เป็น**พฤติกรรมใหม่** (ระบบเดิมไม่เคยลบอะไรที่ปลายทาง) จึงต้องมีเคสทดสอบยืนยันว่าไม่แตะแถว `USER` · ผูกกับข้อ **2.20** (Job 3 กับ `impact_process_id`) และ **2.24** (ช่องทาง STA/PRO/REA ถูกกันออก) |
| **2.31** 🟠 **ปิดข้อ (1) แล้ว 2026-09-13 · เหลือ (2)(3)(4)** | **`status_code` · คีย์กันซ้ำสองชั้น · `data_name` · `forecast`/`adjust` ของ Job 8** | ✅ **(1) `status_code` ปิดแล้ว — ใช้วิธีเดียวกับข้อ 2.17** <br>**ชุดค่าปิด 6 ค่า** `06` รอฝ่าย SBP DSA · `08` รอเจ้าหน้าที่ SBP DSA · `01` รอหน่วยงานส่งเสริมธุรกิจ SBP · `02` รอ GM · `03` รอผู้บริหารสำนักบริหาร SBP (AVP) · `99` เสร็จสิ้น (terminal) — พร้อม `CHECK` และ **`DEFAULT '06'`** <br>**ใครตั้ง:** **Job 8** ใส่ `'06'` ตอนสร้างเอกสาร (ตรงกับ `workflow_status_document.md` บรรทัดแรก "สร้างเอกสาร → รอฝ่าย SBP DSA ดำเนินการ") · หลังจากนั้น **`POST /sgi/document/{docNo}/actions` เป็นผู้เขียนที่เดียวในระบบ** · **Job 8b ไม่แตะคอลัมน์นี้** — ผลการเปิด workflow อยู่ที่ `workflow_generation_status` <br>🔴 **พบของผิดระหว่างเคาะ:** DDL กับเอกสาร lookup เขียนว่า "ค่าจาก `sps_store.workflow_status` ของ engine" แต่ตารางนั้น**มีแค่ `status_id` (integer surrogate) กับ `status_name` (ชื่อไทย) ไม่มีคอลัมน์รหัส 2 ตัวอักษรเลย** → endpoint `/sgi/lookup/document-statuses` ตามที่เขียนไว้เดิม**ทำงานไม่ได้จริง** · แก้เป็นอ่าน `common_code` (`code_type = 'SGI_DOC_STATUS'`) แบบเดียวกับ `SGI_DECISION` พร้อม seed 6 แถว <br>🔴 **แก้ค่าที่หลุดชุด:** `LLDD-API` เคยใช้ `"statusCode": "END"` ในตัวอย่าง response ทั้งที่ที่อื่นใช้ `99` — แก้เป็น `99` แล้ว <br>⚠️ ค่าเดียวกับ `current_section_code` สำหรับสถานะ "รอ..." ทั้ง 5 ตัว ต่างกันแค่ `99` ที่ไม่มี section <br>🔴 **(2)(3)(4) ยังไม่เคาะ:** คีย์กันซ้ำสองชั้นที่ความหมายไม่ตรงกัน (`UNIQUE (impact_process_id)` vs `uq_comp_business` ที่มี `new_store_code`) · `data_name` มีทั้ง `IMPACT_STORE` และ `DOCUMENT_CREATE` ในโดเมน · `forecast`/`adjust` ที่ระบบเดิมยุบเป็นค่าเดียวจนปลายทางแยกไม่ออกว่าตัวเลขมาจากระบบหรือคนปรับ | แก้แล้ว: DDL + `CHECK` + `DEFAULT` · `output/sql/sgi_schema.sql` · seed `common_code` 6 แถว · `LLDD-Database` · `LLDD-API` · `plan-api.html` + `api.md` · ทดสอบติดตั้งบน PostgreSQL 16 จริงแล้ว |
| **2.32** 🔴 **ต้องเคาะ + business sign-off** | **สองเกณฑ์บนตัวเลขเดียวกัน ทำให้แถวค้าง `W` ตลอดไป · และนิยาม "ยอด 0 กี่งวดติดกัน" ยังไม่มี** <br><small>(พบ 2026-09-10 ตอนไล่โค้ด Job 8b)</small> | **(1) เกณฑ์ `growth_rate_diff` ไม่ตรงกัน:** Job 5 ตั้ง `sales_status = 'Y'` เมื่อ `diff < 0` แต่ Gen Flow Gate ของ Job 8b ใช้ `diff <= -10` → **แถวที่ `-10 < diff < 0` ไม่ผ่าน Gate** และ `udateFlagNo()` ก็ไม่ตั้ง `N` ให้ (ตั้งเฉพาะประเภทร้านนอกชุด 6 ค่า) → **ค้าง `flag_gen_flow = 'W'` ตลอดไป** ไม่มีอะไรมาทำให้จบ · ผังใหม่บอกให้เป็น `N` — ต้องยืนยันว่าเป็นการเปลี่ยนพฤติกรรมที่ตั้งใจ และควรรวมสองเกณฑ์เป็นเกณฑ์เดียวหรือไม่ <br>**(2) นิยาม "ยอด 0 กี่งวดติดกัน":** ตรรกะ "ยอด 0 เดือน 1–3 → state 08 · เดือนที่ 4 → หยุดชดเชย" **ไม่มีในระบบเดิมเลย** จึงไม่มีโค้ดให้เทียบ — ต้องนิยามว่านับจากงวดไหนถึงงวดไหน · งวดที่ไม่มีแถวนับไหม · `COALESCE(adjust, forecast) = 0` ต่างจาก `NULL` อย่างไร <br>**(3) `sales_status = 'E'`:** Gate รับ `IN ('Y','N')` — `'E'` (ประเมินผิดพลาด) ไม่ผ่านและไม่ถูกตั้ง `N` → ค้าง `W` เช่นกัน <br>**(4) `opt_dv_user_id` ว่าง → `N` หรือ `W`:** ระบบเดิมคง `W` · ผังใหม่ระบุให้เป็น `N` — ถ้าเป็น `N` แล้ว DV ถูกเติมทีหลัง **เรื่องจะไม่มีวันกลับมา** | กระทบ **เงินโดยตรง** (ข้อ 2 ตัดสินว่าหยุดชดเชยหรือไม่) · ข้อ 1 และ 3 เป็นปัญหาชนิดเดียวกับ "แถวตกร่อง" ของ Job 2 (ข้อ **2.15**) และผูกกับ **2.26** (NULL → Y) · **เกณฑ์ `-10` และชุดประเภทร้าน 6 ค่าเป็น literal ในโค้ด** ต้องย้ายไป `mas_param`/`common_code` · ชุด 6 ค่าของ Job 8b (`FAM FB1 FC1 FB2 FVB FVC`) **ไม่ตรงกับชุดของ Job 2** ที่มี `B` และ `FPT1` ด้วย — ต้องยืนยันว่าตั้งใจ (ดู `batchjob/JOB-08b-StartInternalWorkflow-อธิบายละเอียด.md` หัวข้อ 4 · 10) |
| **2.33** 🔴 **ต้องเคาะ + business sign-off** | **กติกา "%ชดเชยรวมต้องได้ 100" และยอดรายร้านเทียบหัวเอกสาร** <br><small>(พบ 2026-09-10 ตอนไล่โค้ด Job 9)</small> | **ข้อเท็จจริง:** กติกาธุรกิจระบุว่า **%ชดเชยจัดสรรข้ามร้านใหม่ต้องรวมได้ 100% พอดี** แต่ **ระบบเดิมไม่เคยตรวจที่ pipeline เลย** — ค้นทั้ง `ExportController`/`ExportJdbc` ไม่พบการตรวจผลรวม · ความถูกต้องพึ่งหน้าจอ K2 ล้วน ๆ · ระบบเดิมยัง**ไม่ตรวจว่า % เป็น NULL หรืออยู่นอกช่วง 0–100** ด้วย (DDL ใหม่ปิดสองข้อนี้แล้วด้วย `NOT NULL` + `CHECK`) <br>**สิ่งที่ต้องเคาะ:** **(1)** ตรวจก่อนหรือหลัง upsert · ไม่ครบแล้วล้มทั้ง job หรือเฉพาะ `doc_no` · **(2) ยอมให้คลาดเคลื่อนเท่าไร** — `compensate_percent NUMERIC(7,4)` แบ่ง 3 ร้านเท่ากันได้ `33.3333 × 3 = 99.9999` **ถ้าบังคับ `= 100` เป๊ะจะล้มทุกครั้งที่แบ่งไม่ลงตัว** · **(3)** แถวที่ผู้ใช้เพิ่มเองนับรวมไหม · **(4)** prune แล้วต้อง re-validate ไหม (ลบร้านออกหนึ่งร้าน %รวมจะไม่ครบทันที) <br>**(5) ยอดรายร้านเทียบหัวเอกสาร:** ระบบเดิม**ไม่เคยตรวจ**ว่า `SUM(compensation_amount)` ของร้านใหม่ทุกร้าน เท่ากับ `sgi_compensation_documents.total_compensation_amount` ไหม — ต้องเคาะว่าใครเป็นเจ้าของตัวเลข หัวเอกสารหรือรายร้าน | 🔴 **กระทบว่าจ่ายเงินครบหรือไม่ และจ่ายผิดสัดส่วนหรือไม่** · ผูกกับ **2.31** (ข้อย่อย 4 · `forecast`/`adjust` ถูกยุบเป็นค่าเดียว — ที่นี่หนักกว่าเพราะเป็น %ที่กลายเป็นเงินโดยตรง) และ **2.30** (`source_system` · prune) · ⚠️ **Job 9 ไม่กันช่องทาง `STA` ออก** ต่างจาก Job 4/7/8 ที่กันทั้งหมด — ต้องยืนยันว่าตั้งใจ (ผูกกับ **2.24**) · **`DISTANCE_UNIT` ถูก select มาแล้วไม่เขียนลงไฟล์** ผูกกับกติกาแปลงหน่วยของ Job 2 (ดู `batchjob/JOB-09-SyncNewStoreToDocument-อธิบายละเอียด.md` หัวข้อ 6 · 7 · 10) |
| **2.34** 🔴 **ต้องเคาะก่อนเขียน Job 10** | **ขอบเขตที่ watchdog เฝ้า · การนับจำนวนค้าง · กติกาเตือนซ้ำ** <br><small>(พบ 2026-09-10 ตอนไล่โค้ด Job 10)</small> | **(1) ขอบเขตแคบเกินไป:** ระบบเดิมเฝ้าแค่ `COMPENSATE_INIT_I` และ `COMPENSATE_APPROVE_I` · **ไม่เฝ้าฝั่ง `N`** ทั้งที่ Job 6 ส่งเป็นคู่และ RabbitMQ มี publisher confirm **แยกกันแต่ละข้อความ** · **และไม่เฝ้า `IAS_SALES_REQUEST` (Job 4) หรือ `SGI_REFLOW`** ที่ใช้ transactional outbox เหมือนกัน → **Job 4 ที่ค้างจะไม่มีใครเฝ้าเลย** แล้วไปตายที่ Job 5 ที่รอไฟล์ตอบกลับไม่มา <br>**(2) ตัวเลขที่รายงานไม่ใช่จำนวนที่ค้าง:** คิวรีเดิมใช้ `EXISTS` บอกแค่ว่ากลุ่มนี้มีของค้าง แล้วชั้นนอก **นับทุกแถวในกลุ่ม** — คนอ่านต้องเอา `COUNT_DATA − COUNT_RETURN_CODE` เอง <br>**(3) ไม่มี marker กันเตือนซ้ำ:** แถวที่ค้าง 30 วันจะได้เมลเตือนเรื่องเดิม 30 ฉบับ → **alert fatigue** คนกรองเมลนี้ทิ้งแล้วรายการค้างใหม่ถูกมองข้ามไปด้วย · DDL ใหม่มี `last_ack_notified_on` แล้วแต่ยังไม่มีกติกา (วันละครั้ง? ยกระดับเมื่อเกิน N วัน? ใครได้รับตอนยกระดับ?) <br>**(4) พบของค้างแล้วยังรายงาน `SUCCESS`:** รายการค้างถูกใส่เป็น `note` ในเมลสถานะปกติที่ job อื่นก็ส่งทุกวัน → **คำเตือนปนกับเมลประจำวันจนถูกอ่านข้าม** | 🔴 **สิ่งที่ค้างคือข้อความที่ทำให้เงินออก** — ความเงียบตรงนี้แปลว่า **ร้านไม่ได้รับเงินโดยไม่มีใครรู้** · ข้อ 1 + 4 ทำลายจุดประสงค์ของ job ที่มีหน้าที่เดียวคือ "ทำให้ความเงียบดังขึ้น" · **threshold 1 วันเป็น literal ใน SQL** ต้องย้ายไป `mas_param` พร้อมนิยาม `>=` หรือ `>` · **`outbox_status` เป็น nullable** ต้องใช้ `IS DISTINCT FROM` ไม่ใช่ `!=` · ผูกกับ **2.4 (DP-6)** ที่ยังไม่เคาะโครง `sgi_interface_transactions` (ดู `batchjob/JOB-10-NotifyNoReceiveData-อธิบายละเอียด.md` หัวข้อ 4 · 9) |
| **2.35** 🔴 **ต้องเคาะ + ยืนยันกับทีม STA** | **ยอดรายร้านจาก STA ไม่มีที่ลง · นิยาม "รับซ้ำ" · เอกสารที่ปิดแล้ว · ข้อผิดพลาดในสัญญา** <br><small>(พบ 2026-09-10 ตอนไล่สัญญาข้อความของ Job 11)</small> | **(1) `compensate[]` ไม่มีที่ลง:** ข้อความ `sta_update_compensate` ให้ **ยอดและ %แยกรายร้านเปิดใหม่** (`forecastN` · `forecastPercentN` · `adjustN` · `adjustPercentN`) แต่ตารางที่ผังประกาศไว้เก็บไม่ได้ — `sgi_fgi_impact_compensations` มีแค่ยอดรวม · ควรลง `sgi_document_new_stores` **ที่ Job 9 เป็นคนเขียน** → เลือก **(ก) ให้ Job 11 เขียนด้วย** (ชนกับ Job 9 · ต้องระบุว่าใครชนะ) · **(ข) ทิ้ง `compensate[]`** (หน้าจอแสดง %ไม่ตรงที่จ่ายจริง) · **(ค) เพิ่มตารางเก็บยอดที่ STA แจ้งกลับแยก** <br>**(2) "รับซ้ำ" หมายถึงอะไร:** STA ส่ง **Real Time** และอาจส่ง**ยอดใหม่**ของงวดเดิม · กันซ้ำด้วย `UNIQUE (data_name, direction, business_key, period_key)` เฉย ๆ จะ**ทิ้งยอดที่ปรับใหม่** — ต้องแยก "redelivery" ออกจาก "อัปเดตรอบใหม่" <br>**(3) เอกสารที่ปิดแล้ว:** STA อาจส่งยอดกลับหลังเอกสารจบ workflow แล้ว → ตัวเลขบนเอกสารที่ปิดแล้วจะเปลี่ยน <br>**(4) ข้อผิดพลาดในสัญญาต้นฉบับ:** ตัวอย่างระบุ `"sender": "SGI"` ทั้งที่เป็นข้อความ **STA → SGI** · และมี **สองปฏิทินในข้อความเดียว** (`periodImpact` = `"6906"` **พ.ศ.** · `openDateN` = `"30/03/2026"` **ค.ศ.**) <br>**(5) `impactStatus` (`W`/`Z`):** ถ้าจะ map ลง `compensate_status` **`W` ไม่มีในโดเมน** (`CHECK IN ('I','C','A','N','S','Z')`) · แนะนำใช้ตรวจความสอดคล้องแทน (`Z` แต่ยอดไม่เป็นศูนย์ = ข้อความผิด) | ⚠️ **Job 11 ไม่มีโค้ดเดิมใน `fcsJar/` เลย** (ระบบเดิมใช้ web service `/fgiService/updateCompensateFromFS` ที่ STA เรียกเข้ามา — ค้นแล้วไม่พบใน `fcsJar/src/`) จึง**ไม่มีพฤติกรรมจริงให้เทียบ** ต้องขอตัวอย่างข้อความจริงจากทีม STA ก่อน UAT · **`""` ต้องแปลงเป็น `NULL` ไม่ใช่ `0`** เพราะทั้งระบบใช้ `COALESCE(adjust_amount, forecast_amount)` · **DLQ/retry ยังไม่มีในโค้ด `store-consumer`** และ `dataName` ยังไม่อยู่ใน zod schema (**2.12**) — ทดสอบ end-to-end ไม่ได้จนกว่าทีมนั้นแก้ (ดู `batchjob/JOB-11-ConsumeStaCompensate-อธิบายละเอียด.md` หัวข้อ 5 · 9 · 10) |
| **2.36** 🔴 **ต้องเคาะ + business sign-off** | **งานค้างเกิน 66 วันไม่มีใครเตือน · เฝ้าขั้นเดียวจาก 5 ขั้น · นิยาม "วันทำการ"** <br><small>(พบ 2026-09-10 ตอนไล่โค้ด Job 12)</small> | ✅ **(1) ตอบแล้ว 2026-09-16 จาก master จริงของ K2** — ระบบเดิม**ไม่ได้ปล่อยค้าง**: `StatusProfile` รหัส **`10` = ยกเลิกเอกสาร** มีหมายเหตุในฐานเขียนตรงตัวว่า *"สำหรับเอกสารที่ไม่ดำเนินการมาแล้วมากกว่าหรือเท่ากับ **67 วัน** เป็นต้นไป"* และมีผลพิจารณาคู่กันคือ `DecisionProfile` รหัส **`14` = ยกเลิกโดยระบบ** (`CancelBySystem` · หมายเหตุ *"ยกเลิกเนื่องจากรองานเกินระยะเวลาที่กำหนด"*) → **ช่วง `60-66` คือการเตือนครั้งสุดท้ายก่อนถูกยกเลิก ไม่ใช่ช่องโหว่** (ดู `docs/K2-master-data.md`) <br>🔴 **แต่โครงใหม่ยังไม่มีทั้งสองอย่าง** — `SGI_DOC_STATUS` 6 ค่าไม่มี "ยกเลิกเอกสาร" และ `SGI_DECISION` 7 ค่าไม่มี "ยกเลิกโดยระบบ" → ต้องเคาะว่า **จะยกกติกา 67 วันมาด้วยไหม** ถ้ายกมาต้องเพิ่ม 1 สถานะ + 1 ผลพิจารณา + job ที่ยกเลิกให้ · ถ้าไม่ยกมาต้องตอบว่าเอกสารค้างเกิน 67 วันจะจบอย่างไร <br>~~(1) เงียบหลัง 66 วัน:~~ ช่วงเตือนทั้งสามเป็น **ช่วงปิด 7 วัน** (`30-36` · `45-51` · `60-66`) ไม่ใช่ `>= n วัน` · เอกสารที่ค้าง **67 วันขึ้นไปไม่ถูกเตือนอีกเลยตลอดกาล** ทั้งที่นั่นคือกรณีที่ควรดังที่สุด · ช่วง **37-44** และ **52-59** ก็ไม่เตือนเช่นกัน (ออกแบบมาให้พอดีกับ cron รายสัปดาห์ = กันเตือนซ้ำโดยธรรมชาติ) → เลือก **(ก) เพิ่มช่วงที่ 4 แบบ `>= 67 วัน`** · **(ข) คงเดิมแต่รายงาน `over66Count` แยกให้เห็น** <br>**(2) เฝ้าขั้นเดียว:** ระบบเดิมกรอง `PiWfStepId = 501001` **ขั้นเดียว** · ระบบใหม่มี **5 ขั้น** (06 · 08 · 01 · 02 · 03) · **และเอกสารที่ Job 8b ยังไม่เปิด workflow ก็ค้างเหมือนกันแต่ไม่มีใครเฝ้า** <br>**(3) นิยาม "วันทำการ":** K2 คำนวณให้จากปฏิทิน `Franchise Working Day` + **`PiWorkingHourPerDay = 9`** (1 วันทำการ = 9 ชม.) · ระบบใหม่ต้องคำนวณเอง — **ต้องรู้ว่าปฏิทินวันหยุดอยู่ที่ไหนและใช้เกณฑ์ 9 ชม./วัน ไหม** <br>**(4) เอกสารเด้งกลับขั้นเดิม** (ปุ่ม "ส่งกลับฝ่าย SBP DSA") **นับอายุใหม่หรือนับต่อ** <br>**(5) job พลาดหนึ่งสัปดาห์:** ช่วง 7 วันพอดีกับ cron รายสัปดาห์ — ถ้า job ไม่ได้รันสัปดาห์ใด **เอกสารในช่วงนั้นพลาดการเตือนถาวร** ต้องมี alert เมื่อ job ไม่ได้รัน | 🔴 **ปลายทางคือร้านได้เงินช้า** · job นี้เป็น **job อ่านอย่างเดียวตัวเดียวในชุด 12 job** จึงไม่มี marker กันเตือนซ้ำ ต้องพึ่งกลไก "ช่วง 7 วัน" อย่างเดียว · **`sps_store.workflow_transaction` ไม่มี PK และไม่มี index** บน 19,327 แถว → seq-scan ทุกครั้ง (ข้อ **4.1** · DP-2) · **group id 15/38** (OPT/GM) เป็นค่าคงที่ auth-backend เดิม ต้องยืนยันว่ายังใช้ได้ · **ห้ามเรียก `getPendingFlowByUser` ของ lib** (มติ 2026-09-09 — เป็น inbox รายคน แต่ job ต้องการงานค้างทั้งระบบ) และ **ห้ามเขียนตารางของ engine** (ดู `batchjob/JOB-12-NotifyPendingWork-อธิบายละเอียด.md` หัวข้อ 3 · 9) |
| **2.37** ✅ **ปิดแล้ว 2026-09-12** | ~~ค่า `N` ของระบบเดิมไม่มีที่ลงในโครงใหม่~~ → **รับ `N` ตามระบบเดิม + เพิ่ม `P`** ที่ `sgi_fgi_impact_sales_summaries.sales_status` = `W/P/Y/N/E` | **มติ: ไม่แมป `N` → `E`** เพราะเป็นคนละความหมาย <br>**หลักฐาน:** `ImportJdbc.updateStatusImpactStoreSaleByDiff()` บรรทัด 155-180 — `FLAG_VERIFY = CASE WHEN NVL(GROWTH_RATE_DIFF,-1) < 0 THEN 'Y' ELSE 'N' END` · ข้อมูลจริง (`FGI_IMPACT_STORE_SALES_BK_20250515` 8,730 แถว) ยืนยัน: แถว `N` 2,549 แถวมี `growth_rate_diff` **0 ถึง 90.61 และไม่มี NULL เลยสักแถว** = ประเมินสำเร็จแต่ยอดไม่ตก **ไม่ใช่ความผิดพลาด** (แถว `Y` มี NULL 401 แถวเพราะ `NVL(diff,-1)`) <br>⚠️ **แก้ความเข้าใจผิดระหว่างทาง:** ตอนแรกเข้าใจว่าค่านี้ลงที่ `sgi_fgi_impact_stores.sales_request_status` แต่ Source-to-Target ข้อ 5.1 ระบุว่า `FGI_IMPACT_STORE_SALES` → **`sgi_fgi_impact_sales_summaries`** ซึ่ง `sales_status` **มี `N` อยู่แล้ว** · ช่องว่างจริงคือ **ไม่มี `P`** (151 แถวที่ขอแล้วรอผล) <br>`sales_request_status` เป็นคอลัมน์ใหม่ที่ติดตาม *การขอ* (W/P/Y/E) **ไม่มีต้นทางในระบบเดิม** จึงคงโดเมนเดิมไว้ ไม่ใส่ `N` | แก้แล้ว: DDL `sales_status` → `W/P/Y/N/E` · `output/sql/sgi_schema.sql` · `LLDD-Database` · ตารางแมป 5.2.1 · `database.md` + `plan-database.html` · entity ในรีโป batch · `tools/introspect_legacy_oracle.py` (ชี้คอลัมน์ปลายทางให้ถูก — ต้นเหตุของความเข้าใจผิด) |
| **2.38** 🟠 **ปิดข้อ (2)(3) แล้ว · เหลือ (1)** | **ข้อมูลเดิมละเมิดคีย์กันซ้ำ + นิยาม `impact_month` ของแถวแม่** | ✅ **(3) `impact_month` = `START_COMPENSATE_YEAR`+`MONTH`** (2026-09-12) — เท่ากับ `FGI_IMPACT_STORE.YEAR`+`MONTH` ของคู่ร้าน · `ExportJdbc` บรรทัด 684-699 ตั้ง `start_compensate = last_compensate = fis.month/fis.year` ตอน insert · ข้อมูลจริงตรงกัน **7,545 จาก 7,548 แถว (99.96%)** · 🔴 ห้ามใช้ `LAST_COMPENSATE_*` ที่เลื่อนไปแล้ว **49.6%** <br>✅ **(2) กติกาตัดแถวแม่ซ้ำ เคาะแล้ว 2026-09-13 — เก็บแถวที่ `LAST_COMPENSATE` ใหม่สุด** แต่ข้อมูลจริงบอกว่าเกณฑ์เดียว **ตัดสินได้แค่ 2 จาก 9 กลุ่ม** (อีก 7 กลุ่มเหมือนกันทุกคอลัมน์) จึงเติม tie-breaker อีก 2 ชั้น: **`create_date ASC`** (แถว backfill `00:00:00` ชนะแถว job `17:00:xx` — สอดคล้องกับ 2 กลุ่มที่ชั้นแรกตัดสินได้) แล้ว **`impact_process_id ASC`** กันเสมอเด็ดขาด → **ตัดสินครบ 9/9 กลุ่ม** ตัดทิ้ง 9 แถว ทำให้จำนวนแถวแม่ลงตัวที่ **24,783** พอดี <br>✅ **แถวลูกไม่ต้อง repoint** — ลูกของแถวที่ถูกตัดมี **20 แถว** และ **ทุกแถวมีคู่งวดเดียวกันฝั่งผู้ชนะอยู่แล้ว (ไม่ชนกัน 0 แถว) · `compensate_status` ตรงกัน 20 จาก 20 คู่** = เป็นสำเนาซ้ำ 100% ลบทิ้งพร้อมแถวแม่ได้เลย <br>⚠️ ผลข้างเคียงที่ยอมรับ: `last_compensate_seq_no` จะกระโดด (เก็บ `13` ทิ้ง `4`) — **ห้าม renumber** เพราะจะอ้างอิงย้อนหลังกับระบบเดิมไม่ตรง <br>🔴 **(1) ยังไม่เคาะ — คู่ร้านซ้ำ 67 กลุ่ม (ประวัติ) + 93 (live):** 24 กลุ่มเหมือนกันทุกค่า ตัดได้เลย · **43 กลุ่มไม่ใช่ขยะ** — เป็น `ALM`+`flag_verify='N'`+`distance` จริง คู่กับ `STA`+`'Y'`+`distance=0` ทุกกลุ่ม = **การ override โดยมนุษย์** ต้องเขียนกติกา **merge** (เอาสถานะจาก STA + ระยะทางจาก ALM) ไม่ใช่แค่ลบ | กระทบ **migration · Job 2 · Job 8** · กติกาเต็มอยู่ใน `LLDD-BE-Data-Migration-Cutover` ข้อ **5.2.2 / 5.2.3 / 5.2.4** |
| **2.39** 🔴 **ต้องยืนยันกับทีม STA ก่อนเขียน Job 6/11** | **สัญญา `sgi_impact_store` มีสามรูปร่างไม่ตรงกัน · และความหมายของ `S` ขัดกับข้อมูลจริง** <br><small>(พบ 2026-09-16 ตอนเทียบสัญญากับไฟล์ `FRBC0001` จริง 3 ไฟล์ 56 แถว ใน `docs/file_IAS_STA/`)</small> | **(1) สามรูปร่างในเอกสารฉบับเดียว:** ตารางฟิลด์ใน `STA/ประกันรายได้-ตัวอย่าง-Message-RabbitMQ.md` §2.1 มี **18 ฟิลด์** (แยก `COMPENSATE_MONTH`/`COMPENSATE_YEAR` และ `STMT_MONTH`/`STMT_YEAR` + มี `COMPENSATE_I_ID`/`COMPENSATE_N_ID`) · ตัวอย่าง JSON §2.3 มี **14 คีย์** (รวมงวดเป็น `compensate_year_month` · ไม่มี id) · บล็อก "Sample รูปแบบเดิม" มี **13 ฟิลด์** <br>**ไฟล์จริงมี 14 ฟิลด์ และตรงกับ JSON §2.3 ทุกช่องตามลำดับ** → ยึด JSON · 🔴 บล็อก "Sample รูปแบบเดิม" **ตกฟิลด์ `compensate_status` (ตำแหน่ง 8) ไป** — ไฟล์จริงคือ `07925|24192|29/08/2569|1|6908|||I|91.75|80|0|83.33|0|100` มี `I` ที่ตำแหน่ง 8 · ใครลอกบล็อกนั้นไปทำ parser จะเลื่อนคอลัมน์ทั้งแถว <br>**(2) ป้ายชื่อในตาราง §2.1 สลับกัน:** ฟิลด์ 5 ชื่อ `COMPENSATE_MONTH` แต่คำอธิบายเขียน "ปีที่ชดเชย" · ฟิลด์ 6 ชื่อ `COMPENSATE_YEAR` แต่เขียน "เดือนที่ชดเชย" <br>**(3) 🔴 `S` = Stop จริงหรือ:** §2.2 นิยาม `S` = **Stop — หยุดชดเชยประกันรายได้** → STA "Stop Flow" · แต่ข้อมูลจริง **44 จาก 56 แถวเป็น `S` และทุกแถวมี `stmt_year_month` = `6909`** (การหยุดชดเชยไม่ควรผูกงวด statement) · และแถวเดิมเปลี่ยนจาก `I` → `S` ภายในสองวันโดย **คะแนน QSSI เท่าเดิมทุกตัว** (พบ 7 ร้าน) · ฝั่งโค้ดเดิมก็ไม่ตรงกับนิยามนี้ — `decode(compensate_status,'Z','S',...)` **แปลง `Z` เป็น `S` ตอน output** และจัด `A,N,S,Z` อยู่กลุ่ม `COMPENSATE_APPROVE` <br><br>✅ **ยืนยันนิยามได้แล้ว 2026-09-16 จากฐาน K2 เดิม** — `CompensateFlow.CompFlagStatus` ของ `CPA_FRN_FGI` มีคำอธิบายในฐานเขียนไว้ตรง ๆ ว่า `I` อยู่ระหว่างการชดเชย · **`S` หยุดชดเชย** · `N` ไม่ชดเชย&ยกเลิกเอกสาร · **`A` ชดเชย** → นิยามใน §2.2 **ถูกต้องตามระบบเดิมจริง** (ดู `docs/K2-database-CPA_FRN_FGI.md`) <br>🔴 **คำถามที่เหลือจึงแคบลงเหลือสองข้อ:** (ก) ทำไมไฟล์จริง 56 แถว **ไม่มี `A` เลยสักแถว** ทั้งที่ `A` คือสถานะ "ชดเชย" ซึ่งควรเป็นเคสปกติที่สุด · (ข) ทำไม `S` (หยุดชดเชย) ถึงมาพร้อม `stmt_year_month` = `6909` ทุกแถว — ถ้าอ่านว่า "ผูกเงินเข้า statement เสร็จแล้วจึงหยุด flow ฝั่ง STA" ก็สมเหตุผล แต่ต้องให้ STA ยืนยัน เพราะถ้าเข้าใจผิด **เอกสารที่ควรชดเชยต่อจะถูกปิด flow** <br>⚠️ อย่าลืมว่าโค้ดเดิมฝั่ง Oracle **แปลง `Z` → `S` ตอน output** และจัด `A,N,S,Z` อยู่กลุ่ม `COMPENSATE_APPROVE` — `S` ที่ STA เห็นจึงอาจมาจาก `Z` (รอจ่าย/จ่ายครบ) ไม่ใช่ `S` ที่คนกด <br>**(4) เปอร์เซ็นต์หักคำนวณที่ STA ไม่ใช่ที่เรา:** รายงาน `RT040035` แสดงว่า STA เอาคะแนน QSSI 6 ตัวไปคิดเป็น %หัก แล้วลดยอด (`137,446.04 × 81.50% = 112,018.52`) · SGI ส่งแค่คะแนนดิบ → **ยอดสุทธิที่หน้าจอจะแสดงต้องรับกลับจาก STA เท่านั้น ห้ามคำนวณซ้ำ** (ต่อกับข้อ **2.35(1)** ที่ยังไม่มีที่เก็บยอดรายร้าน) | หลักฐานและการถอดโครงสร้างทั้งหมดอยู่ใน **`docs/IAS-STA-interface-files.md`** · ไฟล์ตัวอย่างอยู่ใน `.gitignore` (ข้อมูลธุรกิจจริง) |
| **2.40** 🔴 **ต้องถามทีมเจ้าของ email-lib ก่อนส่งอีเมลฉบับแรกของ SGI** | **ตัวคั่นตัวแปรในข้อความอีเมล — `${ชื่อ}` หรือ `{{ชื่อ}}`** <br><small>(พบ 2026-09-16 ตอนถอด `SBP/TSM-SRM-LLDD SBP EMAIL1.0.xlsx`)</small> | **LLDD ของ lib ยกตัวอย่างชัดว่าใช้ `{{}}`** — `[AD] ExportUserToAD ({{status}})` + `param = {"status":"Success"}` → ได้ `[AD] ExportUserToAD (Success)` <br>**แต่ข้อมูลจริงบนฐาน dev ใช้ `${}` แทบทั้งหมด:** `${}` **126 จาก 134 แถว** และเป็น template ที่ถูกส่งจริง ทุกตัวใน 12 อันดับแรก (รวม 4,800+ ฉบับ) · `{{}}` มีแค่ **2 แถว (id 72 · 153) และ<b>ไม่เคยถูกส่งเลยสักฉบับ</b>** · อีเมลที่ส่งไปแล้ว **5,381 จาก 5,392 ฉบับไม่มีตัวแปรค้าง** = `${}` แทนค่าได้จริง <br>⚠️ **ยังสรุปไม่ได้ว่า `{{}}` ใช้ไม่ได้** เพราะสองแถวนั้นไม่เคยถูกส่ง — ไม่มีหลักฐานทั้งสองฝั่ง · เป็นไปได้ว่า lib รองรับทั้งคู่ · หรือ `${}` เป็นของตัวส่งเมลรุ่นก่อน (K2/ระบบอื่น) ส่วน `{{}}` เป็นของ lib ตัวนี้ · หรือเอกสารเขียนตัวอย่างผิด <br>🔴 **ผลต่อ SGI:** `EM-01…EM-08` ที่ seed ลง dev แล้วใช้ **`${}`** (ตั้งใจให้ตรงกับ 126 แถวเดิม) — ถ้า lib แทนค่าเฉพาะ `{{}}` **อีเมลของ SGI จะออกไปพร้อมตัวแปรดิบ** เช่น `เอกสาร ${docNo} รอท่านดำเนินการ` · ชื่อ key ใน `param` ที่โค้ดส่ง (`jobName` · `errorMessage` · `runId`) **ตรงกับชื่อตัวแปรใน template อยู่แล้ว ต่างแค่ตัวคั่น** <br>**ถ้าคำตอบคือ `{{}}`** แก้ที่ `EMAIL_TEMPLATES` ใน `tools/build_sgi_schema_sql.py` ที่เดียว แล้ว reseed ด้วย `tools/reseed_sgi_seed_rows.py email_template --confirm` | หลักฐานและสัญญาเต็มของ lib อยู่ใน **`docs/SBP-email-lib-LLDD.md`** · ในเอกสารเดียวกันยังมีอีก 4 จุดที่เอกสาร lib ไม่ตรงของจริง (ชื่อคอลัมน์ `email_template` · `sent_by`→`send_by` · `sendEmail`→`sendMail` · SES vs SMTP) |
| **2.41** 🔴 **ต้องถามธุรกิจ — ตัดได้ทั้งฟีเจอร์** | **ฟีเจอร์ "ปัจจัยภายนอก" ไม่เคยถูกใช้เลยตลอด 8 ปี** <br><small>(พบ 2026-09-16 ตอนโหลด dump จริงของ K2)</small> | **ข้อเท็จจริง:** `FactorInCompenProfile` (ตารางที่ผูกปัจจัยภายนอกกับเอกสาร) **เป็นตารางว่าง ไม่มีข้อมูลเลยสักแถว** — ผู้ส่งข้อมูลยืนยันว่าไม่ได้ตกหล่น · ขณะที่ฐานมีเอกสาร **18,007 ฉบับ ปี 2019–2026** <br>แต่ฟีเจอร์นี้มีครบทุกชั้นในระบบใหม่: master 7 รายการ (`sgi_external_factors` · seed ลง dev แล้ว) · หน้าจอดูแล master (`k2-factors.html`) · **4 endpoint** (`/sgi/master/factors` GET/POST/PUT/DELETE) · ตารางผูกกับเอกสาร (`sgi_document_external_factors`) · ส่วนเลือกปัจจัยในหน้าเอกสาร <br>→ ต้องถามว่า **(ก)** ยังต้องมีใน SGI ไหม · **(ข)** เคยใช้แล้วเลิก · **(ค)** ออกแบบไว้แต่ไม่เคยเปิดใช้ <br>**ถ้าตอบว่าไม่ต้องมี ตัดได้ 1 ตาราง + 4 endpoint + 1 หน้าจอ + ส่วนในหน้าเอกสาร** (กระทบยอด 21 ตาราง / 28 endpoint ที่ประกาศไว้) <br>⚠️ ถ้าตอบว่าต้องมี ก็ไม่ต้องแก้อะไร — แค่รู้ว่า migrate มาแล้วตารางจะว่าง | ต่อเนื่องกับอีกสองตารางว่างที่พบพร้อมกัน: `CompDocAttachment` · `CompTempAttachment` (ช่องทาง CM/ECM และไฟล์ชั่วคราว **ไม่เคยถูกใช้**) → ✅ **ไฟล์แนบเหลือทางเดียวคือ `AttachFileProfile`** 11,696 แถว · งาน migrate ไฟล์แนบง่ายกว่าที่ประเมินไว้ · ดู `docs/K2-migration-ผลโหลดข้อมูลจริง.md` |
| **2.42** 🔴 **ต้องถามทีม engine ก่อน deploy workflow** | **รูปแบบ `condition_json` — เอกสารของ lib กับข้อมูลจริงในฐานไม่ตรงกัน** <br><small>(พบ 2026-09-17 ตอน trial-run `sgi_workflow_definition.sql` กับฐาน dev)</small> | **ข้อเท็จจริง (วัดบน dev):** `SBP/TSM-SRM-LLDD-SBP-workflow-1.2.md` บรรทัด 98 เขียน contract ว่าเป็น **object เดี่ยว** `{"field","operator","value"}` พร้อมตัวอย่าง value เป็น**ตัวเลข** `{"field":"amount","operator":"<","value":1000}` <br>แต่แถวจริงใน `sps_store.workflow_route` เป็น **array 16 แถว** `[{"field":"NextApproverStep","operator":"==","value":"602003"}]` · object เดี่ยวแค่ **2 แถว** · และ **value เป็น string ทั้ง 16 แถว ไม่มีตัวเลขสักแถว** <br>→ ถ้า engine เทียบแบบ string เงื่อนไขวงเงินของเราจะ**ผิดเงียบ ๆ** (`'90000' > '100000'` เป็นจริงเมื่อเทียบแบบ string) <br>**ตอนนี้เลือก object + ตัวเลข ตามเอกสารไว้ก่อน** (`CONDITION_SHAPE = "object"` ใน `tools/build_sgi_workflow_sql.py`) — สลับเป็น array ได้โดยแก้ค่าเดียว <br>⚠️ 16 แถวที่เป็น array ใช้ field `NextApproverStep`/`firstApproverStep` ซึ่งเป็น routing ภายในของ engine อาจคนละเส้นทางโค้ดกับเงื่อนไขเชิงธุรกิจ — **ข้อมูลอย่างเดียวตอบไม่ได้** | กระทบ **2 route** ของขั้น 02 (GM) ที่แยกทางด้วยเกณฑ์ 100,000 · เป็นรูปแบบเดียวกับที่เจอใน `docs/SBP-email-lib-LLDD.md` (LLDD ว่า `{{}}` แต่ production ใช้ `${}`) → **เอกสารของ lib ชุดนี้ขัดกับของจริงมาแล้วหนึ่งครั้ง อย่าเชื่อฝ่ายเดียว** |
| **2.43** 🟠 **ต้องถามธุรกิจ — 3 เอกสารไม่มีปลายทางใน SGI** | **เอกสาร K2 3 ฉบับที่ migrate เข้า SGI ไม่ได้** <br><small>(พบ 2026-09-17 ตอนแก้เงื่อนไข migration ตาม review)</small> | **ข้อเท็จจริง:** จาก 18,007 ฉบับ มี 3 ฉบับที่สถานะไม่มีปลายทางในชุด 6 ค่าของ SGI <br>• `2026/02317` — status `04` = **รอฝ่ายบัญชีดำเนินการ** · ขั้นบัญชีถูก SDD v7.5 ตัดออกจาก flow แล้ว เอกสารนี้ยังค้างอยู่ที่ขั้นที่ไม่มีอีกต่อไป <br>• `2025/03337` — status `10` = **ยกเลิกเอกสาร** (ครบ 67 วันไม่ดำเนินการ) · SGI ไม่มีสถานะ "ยกเลิก" <br>• `2023/00001` — ทุกช่องเป็น NULL (แถวขยะ) <br>→ ต้องเลือก **(ก)** ปล่อยไม่ migrate + เก็บไว้ในตารางพัก `sgi_mig_k2_compensate_flow` เป็นหลักฐาน · **(ข)** map `04` และ `10` เป็น `99` (เสร็จสิ้น) ซึ่งบิดความหมาย · **(ค)** เพิ่มสถานะ `10` ยกเลิก เข้าชุดของ SGI <br>**ตอนนี้ทำตาม (ก)** — สคริปต์ข้ามให้ แล้ว `RAISE WARNING` พร้อม**พิมพ์เลขเอกสารทั้ง 3 ออกมา** ไม่ปล่อยหายเงียบ | ถ้าเลือก (ค) กระทบ `CHECK (status_code IN …)` ของ `sgi_compensation_documents` · `common_code` `SGI_DOC_STATUS` (ตอนนี้ 6 ค่า) · หน้าจอตัวกรองสถานะ · เชื่อมกับข้อ **2.36** (กติกา 67 วัน auto-cancel ที่ยังไม่เคาะว่าจะเอามาด้วยไหม) |
| **2.44** 🟠 **ต้องเคาะก่อนย้ายสรุปยอดขาย** | **สรุปยอดขายเป็นของ `process` หรือของ `งวด`** <br><small>(พบ 2026-09-17 ตอนเขียน transform ตารางลูก)</small> | **ข้อเท็จจริง:** `sgi_fgi_impact_sales_summaries` มี `uq_sales_summary_process UNIQUE (impact_process_id)` = **1 process ได้สรุปยอดขายแค่แถวเดียว** <br>แต่ต้นทาง `FGI_IMPACT_STORE_SALES` เก็บเป็น **(ร้าน, เดือน, ปี)** — วัดบน dev แล้วมี **2,170 ร้านที่มีมากกว่า 1 งวด** จาก 8,783 แถว <br>→ ย้ายเข้าตรง ๆ จะเหลืองวดเดียวต่อ process **ที่เหลือหายเงียบ** จึงยังไม่ย้าย (ต่างจากตารางลูกอื่นที่ย้ายแล้ว) <br>**ทางเลือก ก.** เพิ่ม `period_key` เข้า UNIQUE (1 process มีได้หลายงวด) — ตรงกับข้อมูลจริงที่สุด · **ข.** คงเดิม เก็บเฉพาะงวดล่าสุด แล้วยอมรับว่าประวัติยอดขายรายงวดหาย · **ค.** ให้ 1 process = 1 งวด แล้วแตก process เพิ่ม (กระทบ Job 2 ที่ upsert ด้วย `ON CONFLICT (impacted_store_code, impact_month)`) <br>⚠️ `sgi_sales_transactions` เป็นลูกของตารางนี้ (105,930 แถวต้นทาง) จึงติดตามไปด้วยทั้งชุด | กระทบ `sgi_fgi_impact_sales_summaries` · `sgi_sales_transactions` · **Job 3/4/5** ที่เขียน/อ่านสองตารางนี้ · ถ้าเลือก ก. ต้องแก้ DDL + generator + เอกสาร LLDD-Database พร้อมกัน |
| **2.4** | **DP-6** — `sgi_interface_transactions` ออกแบบใหม่ หรือลอกแพตเทิร์น `statement_summary` | — | DDL + การ track สถานะขาออกของ interface (ตั้งแต่มติข้อ 2.13 เกณฑ์คือ publisher confirm ไม่ใช่ ACK ระดับธุรกิจ) |
| **2.7** ✅ **ปิดแล้ว 2026-09-02** | **DP-14** — ค่า `'C'` ของ `sgi_fgi_impact_compensations.compensate_status` | **ตอบจากโค้ดเดิมแล้ว** (`ExportJdbc.insertFgiImpactStoreCompensate` บรรทัด 404/414/452): `C` = **ร้านปิดแล้ว** (`mas_store.close_date <= งวด`) **หรือ สัญญา SBP ถูกยกเลิกด้วย `cancel_type IN ('01','02','03','04','08')` ก่อน/ในงวดนั้น** — ระบบเดิมตั้งค่านี้แทน `I` ตั้งแต่ตอน insert · **ส่ง STA เป็น `S`** (กลุ่มเดียวกับ `Z` — `ExportJdbc` บรรทัด 366/1440 จัด `C`/`S`/`Z` เป็นกลุ่มเดียวกันทุก filter) | ใส่ `CHECK (compensate_status IN ('I','C','A','N','S','Z'))` ลง DDL แล้ว · ตารางโดเมนครบ 6 ค่าอยู่ในหัวข้อ 5.96 ของ `LLDD-BE-Job-6` |
| **2.13** ✅ **ตัดสินแล้ว 2026-09-08 (ทางเลือก ข.)** | **ตัด `POST /sgi/interface/sta/ack` ทิ้ง** แล้วเปลี่ยนนิยาม "ค้าง" ของ Job 10 เป็น **publisher confirm ของ RabbitMQ** <br><small>(พบ 2026-09-08 ตอนไล่ API/DB เทียบกับ repo ที่เพิ่งวิเคราะห์ · ผู้ใช้เลือกทางเลือก ข.)</small> | **หลักฐาน:** สเปก `STA/ประกันรายได้-ตัวอย่าง-Message-RabbitMQ.md` (แปลงจาก .xlsx 3 ชีต) ระบุ **3 ข้อความบน RabbitMQ เท่านั้น** — `sgi_impact_store` (SGI→STA daily) · `sta_update_compensate` (STA→SGI real time) · `sgi_reflow` (SGI→STA) · ค้นคำว่า ack/callback/ตอบรับ ทั้งไฟล์ได้ **0 ครั้ง** <br>**สิ่งที่แก้ตามมติ:** `api.md` + `plan-api.html` **29 → 28 เส้น** (Interface 3 → 2) · `check_docs.py` `CANON_ENDPOINTS = 28` · Job 10 เปลี่ยนเกณฑ์เป็น `direction = 'OUT' AND outbox_status <> 'CONFIRMED' AND created_at < now() - threshold` · DDL ตัดค่า `'ACKED'` ออกจาก `status` และล็อกโดเมน `outbox_status IN (READY,PUBLISHED,CONFIRMED,FAILED)` · partial index `idx_interface_out_pending` เปลี่ยนไปตาม WHERE ใหม่ · purge เหลือ `status = 'COMPLETED'` · Job 5 ขารับกลับใช้ `status = COMPLETED` แทน `ACKED` · `workflow.md` / `plan-flow.html` / `job-batch.html` / `fgi-database.html` / skill md อัปเดตตาม | ✅ **ไม่ต้องรอทีม STA แล้ว** — มติตัดเส้นทิ้ง จึงไม่มีอะไรต้องให้ STA ยืนยัน · ผลพลอยได้: ความเสี่ยงยิงผิดเส้นกับ `/interface/sta/upload-cmadd` ของระบบเดิมหมดไปด้วย |
| **2.11** ✅ **ตัดสินใหม่ 2026-09-12** | **Job 5 / Job 11 consume คิวเองใน `srm-sps-spsap-sop-sgi-batch`** <br><small>(มติผู้ใช้ · **แทนที่มติ 2026-09-08** ที่ให้ `srm-sps-spsap-store-consumer` เป็นตัวรับ — repo นั้นถูกตัดออกจากขอบเขตแล้ว)</small> | **สิ่งที่เปลี่ยน:** EAI/STA ส่งข้อความเข้าคิว → **job bind คิวเอง** ใน `sop-sgi-batch` · **Job 5** อ่าน `urls` จาก envelope แล้วดาวน์โหลดไฟล์จาก S3 เอง (cron เดิมเป็น safety net) · **Job 11** เป็น 1 ข้อความ = 1 หน่วยงาน · **job คุม prefetch · ack/nack · DLQ · retry เอง** | 🔴 **repo ปลายทางมีแต่ `publishMessage` ยังไม่มี consumer เลย** (`SBP/srm-sps-spsap-sop-sgi-batch.md` ข้อ 4 ระบุว่า *"ทุกช่องเป็น publisher อย่างเดียว"*) — **ต้องสร้าง consumer · DLQ · retry policy ใหม่ทั้งชุด** เป็นงานที่ยังไม่ได้ประเมินชั่วโมง |
| **2.12** ✅ **ปิดแล้ว 2026-09-12** | ~~**`dataName` หายจาก envelope ของ `store-consumer`**~~ <br><small>(พบ 2026-09-08 · ปิดเพราะเปลี่ยนสถาปัตยกรรม)</small> | **หมดปัญหาไปเอง** — เมื่อ Job 5/11 consume คิวเองแล้ว (ข้อ **2.11**) **ไม่มีตัวกลางมาตัด `dataName` ทิ้งอีก** · job อ่าน envelope ดิบจากคิวโดยตรง จึงเห็นทุกฟิลด์ที่ต้นทางส่งมา · ตรวจแล้ว **3 แหล่งที่เหลือมี `dataName` ครบ**: `sop-sgi-batch` ✅ · `store-backend` ✅ · สเปก STA ✅ | ⏳ **ยังต้องยืนยันกับทีม STA** ว่าส่ง `dataName` มาจริงตามสเปก — แต่ไม่ใช่ปัญหาของตัวกลางอีกแล้ว |
| **2.10** 🔴 **รอ security sign-off** | **ดาวน์โหลดไฟล์แนบตอนที่ยังไม่มีตัวสแกนไวรัส** <br><small>(พบ 2026-09-07 · เอกสาร 4 ฉบับเคยเขียนกันคนละอย่าง แก้ให้ตรงกันแล้วแต่ตัวนโยบายยังต้องมีคนเซ็นรับ)</small> | **ข้อเท็จจริง:** ค้น `clamav`/`antivirus`/`GuardDuty`/`malware` ใน `store-backend` · `sbp-bff` · `sop-sgi-batch` แล้ว **ไม่พบตัวสแกนเลย** · `POST /statement/upload-file-aws` แค่รับ base64 แล้ววางลง S3 → `scan_status` จะค้างที่ `PENDING` ตลอด <br>**ทางเลือก ก.** บังคับ `CLEAN` อย่างเดียว = ปลอดภัยที่สุด แต่**ไฟล์แนบทั้งระบบดาวน์โหลดไม่ได้เลยจนกว่าจะมีตัวสแกน** (ฟีเจอร์ตายตั้งแต่วันขึ้นระบบ) <br>**ทางเลือก ข. (เขียนไว้ในเอกสารตอนนี้)** สวิตช์ `SGI_ALLOW_PENDING_DOWNLOAD` ใน `mas_param` — `PENDING` ดาวน์โหลดได้เฉพาะช่วงที่ยังไม่มีตัวสแกน พร้อม header `X-SGI-Scan-Status: PENDING` และคำเตือนบนหน้าจอ · `BLOCKED`/`FAILED` ปิดตายเสมอ · พลิกเป็น `N` ได้ทันทีที่ตัวสแกนขึ้น <br>**ทางเลือก ค.** จัดหาตัวสแกน (S3 event + Lambda/ClamAV หรือ GuardDuty Malware Protection) ให้ทันก่อน UAT แล้วใช้ ก. | **BE:** `LLDD-BE-API-Attachment-Sales-Timeline` §5.4 · **FE:** `LLDD-FE-Document-Detail` C06 · **API:** `LLDD-API` เส้น 9 · `mas_param` 1 คีย์ · test case ของ 3 สถานะ <br>🔴 **ทีมพัฒนาตั้งเองไม่ได้** — เป็นการตัดสินใจด้าน security ต้องให้ security/infra เลือกและเซ็นรับก่อน UAT · ถ้าเลือก ข. ต้องกำหนดวันปิดสวิตช์ไว้ด้วย|
| **2.9** ✅ **ปิดแล้ว 2026-09-02** | **งานเตือนงานค้าง + escalation** — สร้าง **`LLDD-BE-Job-12-NotifyPendingWork`** (13 ชม.) แล้ว พร้อมกติกาช่วง 7 วันที่อ่านจากโค้ดเดิม · ⚠️ ข้อค้างที่เหลือ **G7** (วันที่ในอีเมลเป็น พ.ศ. หรือ ค.ศ.) และ **G8** (marker กันส่งซ้ำตอน rerun) <br><small>ของเดิม:</small> **งานเตือนงานค้าง + escalation 30/45/60 วัน** <br><small>(พบ 2026-09-02 ตอนเทียบ LLDD กับ Java)</small> | `SendMailReport.java` / `MailReportService.java` ของระบบเดิม **ไม่ถูกอ้างในเอกสาร LLDD ฉบับใดเลย** ทั้งที่ `workflow.md` และการ์ดแจ้งเตือนใน `k2-flow.html` ระบุว่าต้องมี · ทางเลือก: job ใหม่ `sgi-notify-pending-work` หรือผนวกเข้า `LLDD-BE-Job-Batch-Email-SRM` | **ไม่มีชั่วโมงในยอด 800** · สเปกที่อ่านจากโค้ดเดิมได้แล้ว (ช่วง 30-36 / 45-51 / 60-66 วัน · เฉพาะขั้นรอดำเนินการ · จัดกลุ่มตามโซน · ส่ง GM group 38 + OPT group 15 · วันที่ในเมลเป็น พ.ศ.) อยู่ในหัวข้อ 5.96 ของ `LLDD-BE-Job-10` ใช้ตั้งต้นได้ทันที |
| **2.8** ✅ **ปิดแล้ว 2026-09-02** | **ตัวรับข้อความ `sta_update_compensate` (STA → SGI)** — สร้าง **`LLDD-BE-Job-11-ConsumeStaCompensate`** (16 ชม.) แล้ว · ✅ **ข้อค้างเดิมตกไปแล้วตามมติข้อ 2.11 (2026-09-08)** — เดิมกังวลว่า AWS Batch รัน one-shot จึงต้อง **drain-then-exit ทุก 10 นาที** (near-real-time ไม่ใช่ real-time) · ตอนนี้ `srm-sps-spsap-store-consumer` เป็นผู้ consume คิวแล้ว `SubmitJob` มาที่ Job 11 **1 ข้อความ = 1 การรัน** จึงเป็น real-time ตามสเปก STA และไม่ต้อง confirm เรื่องนี้กับทีม STA อีก <br><small>ของเดิม:</small> **ตัวรับข้อความ `sta_update_compensate`** <br><small>(พบ 2026-09-02 ตอนตรวจ LLDD ของ Job)</small> | สเปก STA · `workflow.md` · `new-flow-improved.mmd` ล้วนบังคับว่า SGI ต้อง **consume** ข้อความนี้เพื่ออัปเดต `forecast_amount`/`adjust_amount` แต่ **ไม่มี LLDD ฉบับไหนรองรับเลย** (RabbitMQ ปรากฏใน 5 ฉบับ ทั้งหมดเป็นฝั่ง publish) · ทางเลือก: ทำเป็นฉบับใหม่ `LLDD-BE-Interface-STA-Consumer` หรือผนวกเข้า Job 5 | **ไม่มีชั่วโมงในยอด 824** · `srm-sps-spsap-sop-sgi-batch` มีแต่ publisher ไม่มีตัวอย่าง consumer ให้ลอก — ต้องออกแบบใหม่จริง |
| **2.5** | **N1** — "หลังพัฒนา **ไม่มีการเปิด SR เพื่อลบข้อมูล**" (SDD สไลด์ 46 · Note) <br><small>(เพิ่มข้อมูล 2026-09-08: ตรวจแล้วพบว่า `sgi_document_attachments.deleted_flag` **ไม่มีใครเขียนเลย** — ไม่มี endpoint `DELETE` ในชุด 28 เส้น และหน้า `k2-document.html` ก็ไม่มีปุ่มลบไฟล์แนบ จึงเป็นคอลัมน์ที่ query ต้องกรอง (แก้แล้ว 2026-09-08) แต่ยังไม่มีทางตั้งค่าเป็น `Y`)</small> | **ยังไม่ระบุขอบเขตเลย** — ลบอะไรได้บ้าง (เอกสาร? แถวร้าน? **ไฟล์แนบ?**) · ใครลบได้ · ต้องมี audit ไหม · **ถ้าตกลงว่าลบไฟล์แนบได้ ต้องเพิ่ม endpoint + ปุ่ม; ถ้าไม่ได้ ควรตัด `deleted_flag` ออกจาก DDL** | หน้าจอ + endpoint ใหม่ที่ยังไม่มีในแผน · ระบบเดิมมี log `TransectionDeleteStore` + `SRNumber` เป็นหลักฐานว่าเคยต้องเปิด SR |

| **2.6** | **PDPA Requirement (SDD สไลด์ 63)** — SDD ระบุว่างานนี้มี Normal Personal Data (ชื่อ-นามสกุล · ที่อยู่/อีเมล/โทรศัพท์) และสั่งให้ *"เพิ่มเติมรายละเอียดตาม Template ของ PDPA Requirement ตามเอกสารแนบ"* | ยังไม่มีเอกสาร PDPA Requirement ในชุดส่งมอบ · ต้องเคาะ **retention ของข้อมูลบุคคล · การ mask ใน log · สิทธิ์เข้าถึง `approver_snapshot` (ชื่อ+อีเมลผู้อนุมัติ)** | ข้อมูลบุคคลในตารางเรา: `consider_by` · `created_by`/`updated_by` · `approver_snapshot` · `file_name` (รายละเอียดใน `database.md` §PDPA) — ที่เหลืออยู่ในระบบ SBP เดิม |

---

## 🟡 กลุ่ม 3 — ตัดสินทีหลังได้ (3 ข้อ)

| # | เรื่อง | หมายเหตุ |
|---|---|---|
| **3.1** | **DP-12** — audit ของ master data: เอากลับมาโดยใช้กลไกของระบบเดิม หรือไม่มีเลยตามมติ 2026-08-07 | `audit_logs` ถูกตัดไปแล้ว |
| **3.2** | **C3 (P2)** — คำนวณชดเชย**แยกตามประเภทร้าน** (`Type A,C` / `Type B,E,V(B)`) + Performance Index · Incentive Guarantee · GP ก่อน Split · ส่วนแบ่งรายได้ตามเป้า (SDD สไลด์ 33 · 36) | **งานใหญ่** ยังไม่ได้ทำเลย · ต้องเคาะขอบเขตก่อน |
| **3.3** | **ขอบเขตหน้างานค้าง (B3)** — จะทำ 2 สถานะก่อนเข้า workflow (`รอมอบหมายงาน` / `คงค้าง`) ในเฟสนี้ หรือเลื่อนไปเฟส 2 | ต้องเพิ่มแหล่งข้อมูลที่ 2 (ร้านที่เข้าเงื่อนไขแต่ยังไม่มีเอกสาร) แล้ว merge เข้ากับ pending list ของ engine · **กระทบทั้ง schema และ `GET /sgi/document/tasks`** · รายละเอียดงานดู [ส่วนที่ 1 ข้อ 1.2](#12--ของที่อยู่ในผัง-to-be--sdd-แต่ยังไม่มีในแบบของเรา) · ส่วน B4/B5/B7/B9 เป็นงานที่ทำได้เลย ไม่ต้องตัดสินใจ |

---

## 📞 กลุ่ม 4 — ไม่ใช่การตัดสินของคุณ แต่ต้องไปถาม/ตาม (6 ข้อ)

| # | เรื่อง | ถามใคร | ความเสี่ยงถ้าไม่ถาม |
|---|---|---|---|
| **4.1** | **DP-2** — `sps_store.workflow_transaction` **ไม่มี PK และไม่มี index เลย** ทั้งที่มี 19,327 แถว (ตารางชื่อเดียวกันใน `sps_auth` มี PK ปกติ) | ทีมเจ้าของ `@srm/glb-workflow` | `GET /sgi/document/tasks` และ `POST /sgi/document/{docNo}/actions` query ตารางนี้**ทุกครั้ง** → seq-scan ทุกคำขอ |
| **4.2** | **engine ส่งอีเมลเองด้วยหรือไม่** บน route ที่มี `email_id` | ทีมเจ้าของ `@srm/glb-workflow` | ถ้าส่งเอง ผู้อนุมัติจะได้ **เมลซ้ำ 2 ฉบับ** (เพราะ SGI เรียก `sendEmail()` เองตามมติ DP-5) |
| **4.3** | **ชื่อ package + transport ของ email-lib** <br><small>(อ่านสเปกจริง `SBP/TSM-SRM-LLDD SBP EMAIL1.0.xlsx` แล้ว 2026-08-24 — **สัญญาเรียกใช้ยืนยันครบ** เหลือ 2 เรื่อง)</small> **(1)** เอกสาร lib **ไม่ได้ระบุชื่อ package** เลย (โครงไฟล์เขียนแค่ `email/`) — `@gosoft-sbp/email-lib` ยังเป็นชื่อที่เดาจากเอกสารอื่น · **(2)** เอกสาร lib **ขัดกันเองในไฟล์เดียว** — ผังไฟล์เขียน *“ส่งเมล์ผ่าน AWS SES”* แต่ลำดับขั้นตอนเขียน *“ส่งเมล์ไปที่ server mail SMTP”*| ทีมเจ้าของ email-lib | ติดตั้ง/เรียกใช้ผิด · ถ้าเป็น SES ต้องมี IAM role + verified identity ซึ่งเป็นงาน infra คนละชุดกับ SMTP |
| **4.4** ✅ **ปิดแล้ว 2026-09-12** | ~~**ALLMAP ใช้ปี พ.ศ. หรือ ค.ศ.**~~ → **ค.ศ.** | **พิสูจน์จากฐาน Oracle จริง** (`10.182.247.47/stqa` · `tools/introspect_legacy_oracle.py`): `FGI_IMPACT_STORE_BK_20250515` **26,264 แถว มีปี 2016–2025 เท่านั้น ไม่มีค่า 25xx แม้แต่แถวเดียว** และคอลัมน์ `YEAR` คัดลอกตรงจาก `PERIOD_YEAR` ของวิว (`ImportStoreMapper.java:39`) จึงเป็นหลักฐานโดยตรงว่า**วิวเก็บ ค.ศ.** · ฝั่งโค้ดก็ตรงกัน — ค่าตั้งต้นมาจาก `Locale.US` (Gregorian) ที่ `DateUtils.java:356` แล้วส่งเข้า `WHERE PERIOD_YEAR = ?` ตรง ๆ ไม่มีการแปลง `543` ที่ใดเลย · ตัวอย่าง `ALL|2569|06` ในเอกสารทีมเป็น**ตัวอย่างที่เขียนผิด** | ตั้ง `SGI_JOB2_ALLMAP_YEAR_ERA=AD` (ค่าตั้งต้นของโค้ดแล้ว) · เก็บ ค.ศ. ลง `sgi_*` เสมอ |
| **4.5** | **`workflow_part` / `workflow_part_display`** ของ engine คุมสิทธิ์แสดงผลรายส่วนหน้าจอ ซึ่ง**ทับซ้อน**กับกลไก `visibleSections`/`editableSections` ที่ SGI ทำเอง | ทีมเจ้าของ `@srm/glb-workflow` | ถ้าใช้ของ engine จะกระทบ `LLDD-FE-Document-Detail` + role pack 5 ฉบับ |
| **4.6** 🔴 **ความปลอดภัย — เร่งด่วน** | **credential ของ prod อยู่ใน source ของระบบเดิม** <br><small>(พบ 2026-09-09 ตอนอ่านโค้ด Job 2 ใน `fcsJar/`)</small> · `fcsJar/jdbc.properties` เก็บ **user/password/URL ของ ALLMAP prod** เป็น plaintext (สลับ QA/PRD ด้วยการ comment) · `fcsJar/src/th/co/gosoft/fgi/Constant/FgiConstant.java:23` เก็บ **domain user + รหัสผ่านของ K2** ในโค้ด · endpoint K2 dev/uat ก็ฝังไว้เช่นกัน | เจ้าของระบบ `fcsJar` + ทีม infra/security | **ย้ายไป Secrets Manager อย่างเดียวไม่พอ — credential ชุดนี้ถูกเปิดเผยแล้ว ต้อง rotate** · ระบบใหม่ (`srm-sps-spsap-sop-sgi-batch`) ต้องรับค่าจาก secret store ตั้งแต่ต้น ห้ามลอกแพตเทิร์นไฟล์ properties มา (ดู `batchjob/JOB-02-ImportImpactStore-อธิบายละเอียด.md` หัวข้อ 11 · L6) |

---

# 🗺️ ลำดับที่แนะนำ

| ลำดับ | ทำอะไร | ข้อ |
|---|---|---|
| **1** | **ตอบก่อนเริ่ม BE** — กระทบ DDL โดยตรง | `1.1` DP-11 (ข้อเดียวที่บล็อกการเริ่ม BE) |
| **2** | **ยิงคำถามคู่ขนานได้เลย** ไม่ต้องรอข้ออื่น | กลุ่ม 4 ทั้ง 5 ข้อ |
| **3** | **ก่อนเริ่ม FE** | `2.1` DP-13 · `2.2` DP-13b |
| **4** | ~~ลุยงานที่ไม่ต้องรอใคร~~ **ทำแล้ว 2026-08-24** | ~~B4 · B6 · B7 · B9 · C4 · C5~~ |
| **5** | **เคาะขอบเขตเฟส 2** | `3.2` C3 คำนวณแยกประเภทร้าน · `2.5` N1 การลบข้อมูล · `3.3` B3 หน้างานค้าง |

---

# ✅ ประวัติการปิดข้อค้าง

> ### ✅ ปิดเพิ่ม 5 ข้อ (2026-08-24) — ตัดสินได้จาก SDD เอง หรือจากมติที่ให้ไว้แล้ว ไม่ต้องถามใหม่
> | ข้อ | มติ | หลักฐาน |
> |---|---|---|
> | **B8** · To-Be ยังมีขั้น AVP (03) ไหม | **มี — คงขั้น 03 ไว้ ไม่ต้องแก้อะไร** | มติ 2026-08-18 (เกณฑ์เดียว 100,000 · ≥ 100,000 → AVP) เป็นมติที่ให้ไว้แล้ว + SDD สไลด์ 54 · 55 · 62 มี AVP · ผัง To-Be `120226` ตกหล่นข้างเดียว |
> | **D6** · ชื่อสถานะขั้น 03 | **ใช้ของเดิม `รอผู้บริหารสำนักบริหาร SBP ดำเนินการ`** ไม่ rename | SDD เรียกหน่วยงานนี้ว่า **"สำนักบริหาร Store Business Partner" 4 ครั้ง** ในตารางผู้เกี่ยวข้อง (Project Sponsor/Owner/BPM/Business User) ส่วน "สำนักงานบริการ" โผล่**ครั้งเดียว**ในลิสต์สถานะสไลด์ 62 → เป็น typo ของ SDD ไม่ใช่มติเปลี่ยนชื่อ |
> | **DP-7** · `sgi_consideration_logs` ตารางเราเอง หรือต่อยอด `workflow_history` | **ตารางของเราเอง** (ผูก `transaction_id` ของ engine) | ตัดสินไปแล้วตั้งแต่รอบ reuse 2026-08-06 · บันทึกอยู่ใน `database.md` — engine ไม่มี `decision_code` / `result_category` / ไฟล์แนบต่อการพิจารณา · เป็นรายการที่ตกค้างในทะเบียนทั้งที่มีมติแล้ว |
> | **DP-8** · `sgi_document_attachments` ตารางเราเอง หรือต่อยอด `upload_general` | **ตารางของเราเก็บ metadata · ใช้ service S3 ของระบบเดิม ไม่เขียน storage layer เอง** | ตัดสินไปแล้ว 2026-08-06 · บันทึกอยู่ใน `database.md` (`upload_general` ขาดฟิลด์ที่ต้องใช้) |
> | **B2** · 3 ช่องทางต้นทาง (เชิงรุก/เชิงรับ/ระบบ) | **ตั้งรหัสใหม่ `PRO` = เชิงรุก · `REA` = เชิงรับ** เพิ่มเข้า `sgi_fgi_impact_processes.datasource` (เดิมมี `ALM`/`STA`) · seed ที่ `common_code` code_type `SGI_DATASOURCE` | ตรวจแล้ว**ไม่มีที่ไหนกำหนดรหัสไว้** — SDD 0 hit · Java เดิมมีแค่ `ALM`/`STA`/`HRS` · ไม่มีข้อมูลฝั่งระบบเดิม → ตั้งตามแพตเทิร์น 3 ตัวอักษรของเดิม (อนุมัติ 2026-08-24) |
> | **C1 / C2** · checkbox เลือกหลายเอกสาร + ปุ่ม Preview Report / Export CSV to Batch | **ทำแล้ว 2026-08-24** | SDD ระบุตรง ๆ ไม่ต้องตีความ — สไลด์ 48 *"แสดง Chk list สำหรับเลือกดำเนินการหลายเอกสาร"* + *"หน้าจอ popup เมื่อยืนยัน"* · สไลด์ 62 ปุ่ม **Preview Report** และ **Export CSV to Batch** |


> ### ✅ ปิดไปแล้ว 3 ข้อ (2026-08-21)
> | ข้อ | มติ |
> |---|---|
> | **DP-10** · SGI อยู่ที่ไหน | **ใส่ใน `srm-sps-spsap-store-backend` เดิม** — ไม่แยก backend ใหม่ |
> | **F8 + F1** · ขนคอลัมน์/ตารางจากระบบเดิม | **ทำ** — เพิ่มตาราง `sgi_fgi_impact_compensations` + 5 กลุ่มคอลัมน์ใน `sgi_fgi_impact_processes` · โครงเป็น **20 ตาราง** |
> | **ชั่วโมงงาน** | **ใช้ชุด LLDD `829 ชม.` @ 8.5 ชม./วัน (กรอบใหม่ 2026-08-25 · 5 วัน/สัปดาห์ × 4 สัปดาห์ = 170 ชม./คน)** (→ **800 ชม.** หลังตัด Job 1 เมื่อ 2026-08-24) (823 + 22 หลังรับ F8+F1 และงานจุดเข้า flow) · ตรวจแล้วสองชุดขอบเขตเดียวกัน ต่างกันแค่หน่วย (142.0 vs 140.8 man-day = 0.8%) · ไฟล์ estimate เดิมเป็น baseline superseded |

> เอกสารนี้รวม**เฉพาะข้อที่ยังไม่ตัดสิน** · ข้อที่ตัดสินไปแล้ว (**DP-1 · DP-3 · DP-4 · DP-5 · DP-7 · DP-8 · DP-9 · DP-10** — ปิด 8 จาก 12 · วงเงิน 100,000 · ลำดับ 06→08→01 · คำเรียกประเภทร้าน) บันทึกอยู่ใน living docs ตามเดิม · **ที่ยังค้างจริงคือ DP-2 · DP-6 · DP-11 · DP-12**
